# tests

## 负责

- 承载单元、集成、smoke 与契约验证相关测试资产。
- 定义跨平台验证入口并记录最小可复现输入集合。

## 不负责

- 不负责运行时核心实现与代码生成实现。
- 不负责第三方源码版本登记。

## 与其它目录的边界

- 以 `contracts/` 为断言基准，覆盖 `analysis/`、`codegen/`、`runtime/` 与 `build/`。
- 测试运行输出与日志收口到 `artifacts/`，不反向污染源码目录。
