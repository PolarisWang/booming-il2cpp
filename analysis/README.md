# analysis

## 负责

- 承载静态分析、语义分析与中间表示分析相关能力。
- 为 `codegen/` 和验证流程输出可消费的分析结果。

## 不负责

- 不负责前端输入适配和 UI 侧集成代码。
- 不负责平台打包、签名与分发流程。

## 与其它目录的边界

- 输入来源由 `frontend/` 或测试用例提供。
- 输出被 `runtime/`、`codegen/`、`tests/` 消费，契约由 `contracts/` 约束。
