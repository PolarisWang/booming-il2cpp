# Generated by chaos-il2cpp convert-to-cpp --sdk-out
# MSVC version: 19.42.34435
# chaos-config.cmake — chaos-il2cpp SDK find_package entry point
#
# Usage:
#   find_package(chaos REQUIRED PATHS path/to/chaos-sdk)
#   target_link_libraries(my_app PRIVATE chaos::codegen chaos::runtime)
#
# Exposed targets:
#   chaos::runtime  — INTERFACE: headers + prebuilt runtime libs + compile flags
#   chaos::codegen  — IMPORTED: precompiled generated code .lib

cmake_minimum_required(VERSION 3.20)

# SDK root is the directory containing this file
get_filename_component(CHAOS_SDK_ROOT "${CMAKE_CURRENT_LIST_DIR}" ABSOLUTE)

# Load target definitions
include("${CHAOS_SDK_ROOT}/cmake/chaos-targets.cmake")

# Convenience variables for consumers that reference SDK paths directly
set(CHAOS_INCLUDE_DIR "${CHAOS_SDK_ROOT}/include")
set(CHAOS_LIB_DIR "${CHAOS_SDK_ROOT}/lib")