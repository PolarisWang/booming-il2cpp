# Roadmap 0 详细规划讨论记录

**日期：** 2026-03-31 23:57:04 +08:00

**状态：** 已形成详细计划草案，待用户复核

## 用户原始请求

用户希望基于现有 `plan-2026-03-31-23-il2cpp-like-cs-to-cpp-implementation-roadmap.md` 中的 `Roadmap 0`，单独做一份“全新的、详细的规划”，用于指导该阶段的实际落地。

## 当前上下文

- 已有一份项目级路线图，定义了 `Roadmap 0` 到 `Roadmap 6` 的分阶段推进方式。
- `Roadmap 0` 的定位不是实现真实运行时或真实翻译器，而是冻结：
  - 仓库骨架
  - 契约产物格式
  - Runtime ABI v0
  - Codegen Bridge API v0
  - Preserve/descriptor 格式
  - Smoke 输入资产
  - Android/iOS 早期验证矩阵
- 当前仓库几乎没有实现代码，说明本阶段应优先产生“可执行的工程骨架 + 契约 + 自动化校验入口”，而不是提前进入 Roadmap 1/3 的实装。

## 关键判断

1. 这次请求目标明确，不需要再回到 brainstorm 阶段。
2. 这是一项典型的复杂规划任务，适合直接使用 `writing-plans` 产出独立计划文档。
3. `Roadmap 0` 的核心不是“先建很多空目录”，而是把后续 Roadmap 1-4 会共享的边界正式化。
4. 如果 `Roadmap 0` 计划不细化到文件级和验证命令级，后续执行时很容易再次发散。
5. Android/iOS 在 `Roadmap 0` 中不应要求完整语义验证，但必须纳入：
   - toolchain/build 描述
   - compile/link smoke
   - host harness 形态定义

## 本轮规划目标

输出一份独立的 `Roadmap 0` 实现计划，覆盖以下问题：

- 要创建哪些目录和文件
- 哪些文档是契约正文，哪些文件是验证夹具
- 哪些东西属于 schema，哪些属于 ABI，哪些属于 bridge
- 如何验证每一类产物
- 何时把 Android/iOS 作为 compile-only 目标，何时升级为 smoke 目标

## 本轮结论

- 新计划应保持在原项目目录下，形成与现有总路线图并列的独立子计划。
- 计划正文采用 `## Chunk N:` 分块结构，便于后续按块执行和审查。
- 详细计划应至少覆盖 5 个块：
  1. 仓库骨架与契约索引
  2. Artifact schema pack
  3. Runtime ABI v0 与 Codegen Bridge v0
  4. 构建工具链与移动端验证骨架
  5. Smoke 资产与 Roadmap 0 本地校验入口

## 讨论日志

- 2026-03-31 23:57:04 +08:00：记录用户对 `Roadmap 0` 独立详细规划的请求。
- 2026-03-31 23:57:04 +08:00：确认现有总路线图已足够清晰，本轮无需再次做需求发散。
- 2026-03-31 23:57:04 +08:00：确认 `Roadmap 0` 的产物应以“契约与骨架”为主，而不是提前实现运行时或前端逻辑。
- 2026-03-31 23:57:04 +08:00：确认 Android/iOS 需要纳入 `Roadmap 0`，但目标层级应限定在构建与启动 smoke 的前置准备。
