# 2026-04-17 13:30 Progress

## 本次推进

- 正式从 `Phase 0` 切换到 `Phase 1`
- 承接已完成的 collection schema versioning 与 feature authority cutover
- 把本阶段的主任务聚焦为：
  - generated host contract rebuild
  - file-level template boundary cutover
  - collector responsibility cleanup

## 当前判断

- 现阶段继续扩大 subject 内容重构还太早，先把 framework contract 和 generator 边界收紧更关键
- 如果不先锁 generated host 的 contract，后续模板化只会把旧耦合复制一遍
- collector 边界要尽快明确，否则测试框架治理会继续和 IL2CPP 核心代码混杂
