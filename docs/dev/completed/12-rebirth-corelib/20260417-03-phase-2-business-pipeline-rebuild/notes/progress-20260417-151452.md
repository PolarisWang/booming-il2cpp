# Progress 2026-04-17 15:14:58 +08:00

## 本轮完成

- `subject_workers.py`
  - `collection-backed` 的 structured benchmark host 不再透传 `--workload-entry`
  - managed/interpreter benchmark host 现在只靠 `collectionPath + entryIndex (+ binding manifest)` 路由
- 测试
  - 更新 `tests/unit/execution/test_subject_workers.py` 的 4 个 structured benchmark host 用例
  - 更新 `tests/integration/run/test_perf_harness_hotupdate_benchmark_host.py`
  - 局部回归 `37 passed`
  - 扩展交叉回归 `167 passed`

## 影响

- structured benchmark host 的真实字符串协议面继续缩小
- `workloadEntry` 在这条路径上退回为结果证据字段，而不再是执行路由输入

## 下一步

- 审计 `native dispatch manifest` 中 `workloadEntry` 的真实必要性
- 审计非结构化反射 fallback 是否可以完全依赖 declared metadata
