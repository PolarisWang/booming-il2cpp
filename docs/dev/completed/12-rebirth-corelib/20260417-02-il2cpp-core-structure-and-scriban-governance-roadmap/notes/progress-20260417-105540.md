# 2026-04-17 10:55 Progress

## 本轮动作

- 把 `NativeAotLoweringPlanner` 的 method emission、external runtime helpers、object model emission 从 root planner 连续区段搬移到 `Emission/` 与 `RuntimeSupport/`。
- 继续把 `Emission/NativeAotLoweringPlanner.MethodEmission.cs` 细分为：
  - `Emission/NativeAotLoweringPlanner.MethodEmission.cs`
  - `Emission/NativeAotLoweringPlanner.ExceptionEmission.cs`
- 同步修正结构治理 compatibility 测试与 `Phase4B` object model 文本 contract，使其校验 partial-file 真相源，而不是要求 root planner 保留实现细节。

## 结果

- `NativeAotLoweringPlanner.cs` 已收缩到 `82372` bytes。
- `MethodEmission.cs` 已收缩到 `127873` bytes。
- `ExceptionEmission.cs` 新增，当前 `86922` bytes。
- 已确认 `NativeAot` 主线仍可构建，且关键 compatibility 套件保持绿灯。

## 验证

- `python -m pytest tests/unit/compatibility/test_il2cpp_codegen_structure_governance.py tests/unit/compatibility/test_phase4b_aot_core_ir_object_model.py tests/unit/compatibility/test_capability_driven_lowering.py tests/unit/compatibility/test_async_await_proof_subject.py tests/unit/compatibility/test_engine_host_proof_subject.py tests/unit/compatibility/test_engine_binding_subjects.py tests/unit/compatibility/test_mainline_capability_batch_a.py tests/unit/compatibility/test_mainline_capability_batch_b.py tests/unit/compatibility/test_marshaling_proof_subject.py tests/unit/compatibility/test_nested_exception_proof_subject.py tests/unit/compatibility/test_threading_gc_proof_subject.py -q`
- 结果：`40 passed`
- `dotnet build src/managed/Chaos.IL2CPP.CodeGen/Chaos.IL2CPP.CodeGen.csproj -c Release -m:1`

## 观察

- 这轮验证了：`NativeAot` 结构切片可以继续沿着 `root orchestrator + partial family` 的方向推进，不需要再把实现回流到 root planner。
- 反编译恢复已编译实现时，单文件模式会引入 compiler-generated helper 名称；如果后续还需要从已编译 DLL 回收代码，必须优先使用 `ilspycmd -p` 的可编译工程模式。

## 下一步

- 继续拆 `ObjectModelEmission` 与 `ExternalRuntimeHelpers` 的内部 helper 家族。
- 开始进入 `NativeReferenceLoweringPlanner` 与 `LoaderStage` 的下一批结构切片。
