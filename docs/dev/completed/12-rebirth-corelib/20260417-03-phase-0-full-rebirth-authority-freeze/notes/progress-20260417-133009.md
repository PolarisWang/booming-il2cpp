# 2026-04-17 13:30 Progress

## 本次推进

- 补齐 3 个 canonical subject 的 `subject.features.json` owner capability entries
- 为 `capability_coverage.py` 新增文件 authority 红测
- 将 capability coverage 从硬编码表切换为消费 `subjects/*/subject.features.json`
- 跑通 Phase 0/1 相关回归并确认无新增回归

## 关键结果

- `subject.features.json` 不再只是空壳种子文件，已经能驱动 owner/support/proof/benchmark contract 解析
- registry / dashboard / reporting 继续通过 `resolve_capability_contract(...)` 取值时，底层真源已切换到文件 authority
- Phase 0 可以正式收口，Phase 1 可以直接从 generated host contract 和 collector boundary 继续推进

## 对后续的直接影响

- 后续不应再向 `capability_coverage.py` 回填硬编码表
- `subject.features.json` 将成为 Phase 2 以后 coverage/report pipeline 的持续输入
- generated host/template 改造必须与这套 authority 链保持单轨一致
