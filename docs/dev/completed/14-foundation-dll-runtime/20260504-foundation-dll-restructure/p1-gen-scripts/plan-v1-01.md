# P1 Gen-Scripts 实现计划

> **面向执行 Agent：** 必须使用 dev:subagent-driven-development（如果可用子 Agent）或 dev:executing-plans 来执行本计划。步骤使用复选框（`- [ ]`）语法跟踪。执行完成时必须先归档当前子任务目录，再同步父任务。

**目标：** 改造 4 个 Python generator 脚本，使其输出到新目录结构（src/, src/patch/, managed_test/, solution/, il2cpp_dist/, native_test/, solution_native/）

**架构：** 保持每个 generator 的入口签名不变（`generate_family_skeleton`、`generate_execution_projects`、`generate_execution_solution` 等），只改内部输出路径、模板内容和文件组织方式。上游调用者（`gap_analyzer.py`）无需修改。

**技术栈：** Python 3.10+, pathlib, 字符串模板

**架构审核模式：** normal

**结构告警重点：** 文件职责与拆分 — 确保 auto-generated 源码合并到 src/ 下的单一文件，不再分散到 benchmark/host/test 目录；native_codegen_generator 职责扩大到 il2cpp_dist + native_test + solution_native，注意保持函数内聚

**权责图审核主题：** n/a

**AOT/IL2CPP/Test Governance Intake：**
- capabilityFamily: foundation-dll
- capabilityItem: gen-scripts
- ownerSubjectId: SolutionCorePack
- proofRequired: true
- benchmarkRequired: false
- hotupdateImpact: None
- formalVerificationObjects: [generator Python scripts, generated .cs/.csproj/.sln]
- requiredGates: collector -> registry -> workspace

**设计文档：** n/a（设计摘要已在父 STATUS.md 中确认）

**问题清零来源：** parent-roadmap（父 roadmap 已获批，设计摘要已确认）

**计划来源：** roadmap-child-auto

**预期知识沉淀：** n/a

**收尾约束：** 执行完成后必须进入"结构告警与架构审视 -> 测试通过 -> 归档 completed -> 合并&提交"固定链路。

---

## 文件清单

### 修改的文件

| 文件 | 职责 |
|------|------|
| `build/toolchains/run/testing/foundation_dll/test_code_generator.py` | 修改 `generate_family_skeleton` 输出路径和模板内容 |
| `build/toolchains/run/testing/foundation_dll/execution_project_generator.py` | 修改 `generate_execution_projects` 输出到 `solution/`，减少到 4 个项目 |
| `build/toolchains/run/testing/foundation_dll/execution_solution_generator.py` | 修改 `generate_execution_solution` 输出到 `solution/`，加 solution folders |
| `build/toolchains/run/testing/foundation_dll/native_codegen_generator.py` | 修改输出到 `il2cpp_dist/` + 新增 `native_test/` 和 `solution_native/` 生成 |

### 不改的文件

- `gap_analyzer.py` — 调用签名不变，只消费返回的 artifacts 路径
- `run_phase2.py` — 只引用 `test_code_generator` 工具函数，不改
- 单元测试文件 — 后续 P2 迁移时一起更新

## 详细改动

### 1. `test_code_generator.py`

#### 1.1 `generate_family_skeleton()` 输出路径变更

当前：
```
family_root/test/<Class>.cs                          ← handwritten
family_root/test/<Class>.AutoGenerated.cs             ← auto test methods
family_root/benchmark/<Class>Benchmarks.AutoGenerated.cs
family_root/host/<Class>HotUpdate.AutoGenerated.cs
family_root/patch/<Class>Patch.AutoGenerated.cs
```

改为：
```
family_root/src/<Class>.cs                            ← handwritten (partial class)
family_root/src/<Class>.AutoGenerated.cs              ← auto: benchmark/host/test 方法合并
family_root/src/patch/<Class>Patch.cs                 ← handwritten (partial class, new)
family_root/src/patch/<Class>Patch.AutoGenerated.cs   ← auto: patch 方法
family_root/managed_test/tests/<Class>.Tests.cs       ← test exe 源码 (new)
family_root/managed_test/benchmarks/<Class>.Benchmarks.cs  ← benchmark exe 源码 (new)
```

#### 1.2 `_handwritten_source()` 新内容

当前：生成包含 `[CapabilityTest]` + `using Xunit` 的 test class。
改为：生成纯 partial class 声明，无 test 依赖：

```csharp
using System;
// (other base usings as needed)

[CapabilityFamily("convert-char")]
public partial class ConvertChar
{
}
```

#### 1.3 `_generated_source()` 新职责

当前：生成 xunit `[Fact]` 测试方法。
改为：生成包含以下内容的 partial class：
- `[BenchmarkSubjectId]` + benchmark 方法（来自旧的 `_benchmark_generated_source`）
- `[HotUpdateSubjectId]` + hotupdate 方法（来自旧的 `_hotupdate_generated_source`）
- `[MethodSubjectId]` + 测试方法（来自旧的 `_generated_source` 中的 test body）

签名保留：`_generated_source(family_id, display_name, class_name, *, method_subject_ids, capability_family_enum)`

内部将 benchmark/host/test 三类方法输出到同一个文件，同一个 `public partial class`。

#### 1.4 新增 `_patch_handwritten_source()`

生成 `src/patch/<Class>Patch.cs`（仅当文件不存在时写入）：

```csharp
using System;

public static partial class ConvertCharPatch
{
}
```

#### 1.5 新增 test exe 源码生成

`managed_test/tests/<Class>.Tests.cs` 内容：

```csharp
using System;
using System.Reflection;
using Xunit;

public class ConvertCharTests
{
    [Fact]
    public void AutoGeneratedMethods_ShouldExecuteWithoutThrow()
    {
        var methods = typeof(ConvertChar).GetMethods(BindingFlags.Public | BindingFlags.Static | BindingFlags.Instance)
            .Where(m => m.GetCustomAttribute<MethodSubjectIdAttribute>() != null);
        foreach (var method in methods)
        {
            // Invoke with default args
            var instance = method.IsStatic ? null : Activator.CreateInstance(typeof(ConvertChar));
            method.Invoke(instance, null);  // simplified — real impl may need parameter generation
        }
    }
}
```

（实际模板内容可更完善，但核心是通过反射发现 `[MethodSubjectId]` 方法并调用）

#### 1.6 新增 benchmark exe 源码生成

`managed_test/benchmarks/<Class>.Benchmarks.cs` 内容：

```csharp
using System;
using System.Diagnostics;
using System.Reflection;

class Program
{
    static void Main()
    {
        var methods = typeof(ConvertChar).GetMethods(BindingFlags.Public | BindingFlags.Static)
            .Where(m => m.GetCustomAttribute<BenchmarkSubjectIdAttribute>() != null);
        // Warmup + measurement loop
        // Output JSON timing results
    }
}
```

#### 1.7 `_patch_generated_source()` 不变

但输出路径改为 `src/patch/<Class>Patch.AutoGenerated.cs`。

#### 1.8 删除的生成

- 不再生成 `benchmark/<Class>Benchmarks.AutoGenerated.cs`
- 不再生成 `host/<Class>HotUpdate.AutoGenerated.cs`
- 不再生成独立的 csproj（移交给 execution_project_generator）

#### 1.9 `generate_family_skeleton()` 内部调整

```python
# 新路径
handwritten_path = family_root / "src" / f"{class_name}.cs"
generated_path = family_root / "src" / f"{class_name}.AutoGenerated.cs"
patch_handwritten_path = family_root / "src" / "patch" / f"{class_name.replace('Tests', 'Patch')}.cs"
patch_generated_path = family_root / "src" / "patch" / f"{class_name.replace('Tests', 'Patch')}.AutoGenerated.cs"
test_exe_path = family_root / "managed_test" / "tests" / f"{class_name}.cs"
benchmark_exe_path = family_root / "managed_test" / "benchmarks" / f"{class_name.replace('Tests', 'Benchmarks')}.cs"
```

### 2. `execution_project_generator.py`

#### 2.1 输出路径

当前：`family_root/test/<Project>.Tests.csproj` 等
改为：`solution/<assembly>/<family_slug>/<Project>.csproj` 等

#### 2.2 4 个 project 类型（代替原来的 5 个）

| 类型 | OutputType | 引用 | 说明 |
|------|-----------|------|------|
| `src/<Project>.csproj` | Library | Chaos.TestFramework.Sdk | 核心 class library，含 src/*.cs |
| `patch/<Project>.Patch.csproj` | Library | 无（独立编译） | patch DLL，含 src/patch/*.cs |
| `tests/<Project>.Tests.csproj` | Exe | src project + xunit | 测试 exe，含 managed_test/tests/*.cs |
| `benchmarks/<Project>.Benchmarks.csproj` | Exe | src project | benchmark exe，含 managed_test/benchmarks/*.cs |

#### 2.3 相对路径调整

csproj 在 `solution/<assembly>/<family_slug>/`，引用源码要回退到 `../../../<assembly>/<family_slug>/src/`。

#### 2.4 新增模板

`_src_project_template(name)`:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>{name}</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../../../reference/Chaos.TestFramework.Sdk/Chaos.TestFramework.Sdk.csproj" />
  </ItemGroup>
</Project>
```

`_patch_project_template(name)`:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>{name}</AssemblyName>
  </PropertyGroup>
</Project>
```

`_tests_project_template(name, src_ref)`:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>{name}</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="{src_ref}" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>
</Project>
```

`_benchmarks_project_template(name, src_ref)`:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>{name}</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="{src_ref}" />
  </ItemGroup>
</Project>
```

#### 2.5 `generate_execution_projects()` 函数调整

签名不变，内部：
- `solution_dir = repo_root / "solution" / assembly_name / family_slug`
- 生成 4 个 csproj，分别放在 src/patch/tests/benchmarks 子目录
- src_ref 从 solution 目录回退到 family 源码目录
- 不再生成 native/host 项目
- 不再删除旧文件（旧路径已不存在）

### 3. `execution_solution_generator.py`

#### 3.1 输出路径

当前：`verification/foundation-dll/FoundationDllTranslationSolution.sln`
改为：`solution/FoundationDllTranslationSolution.sln`

#### 3.2 Solution folders

每个 family 作为一个 solution folder（按 family slug 命名），4 个项目放进去。

#### 3.3 项目引用路径

从 `solution/` 到 `solution/<assembly>/<family_slug>/` 下各 csproj 的路径。

#### 3.4 `generate_execution_solution()` 签名不变

`solution_path` 参数改为从 caller 传入，但 gap_analyzer 中调用时要改路径。

注意：`gap_analyzer.py:292-298` 中 hardcoded 了 sln 路径，改为 `repo_root / "solution" / "FoundationDllTranslationSolution.sln"`。

### 4. `native_codegen_generator.py`

#### 4.1 `generate_native_skeleton()` 输出到 `il2cpp_dist/`

当前：`family_root/native/RuntimeSkeletonSummary.cpp` + page files + CMakeLists.txt + coverage.json
改为：输出到 `family_root/il2cpp_dist/`：
- `native-aot.generated.cpp` — 实际 il2cpp 翻译产出（原 RuntimeSkeletonSummary.cpp 逻辑，但更名为 native-aot.generated.cpp）
- page 文件不需要了（il2cpp 产出是单一文件）
- `metadata.h` — 元数据头文件
- `runtime_helper_shapes.h` — 运行时辅助类型
- coverage.json 保留

**简化处理**：由于 P1 只改 generator 输出路径，实际的 il2cpp 翻译（P3）才生成真正的 native-aot.generated.cpp。P1 中 `generate_native_skeleton()` 改为输出 stub 文件到 `il2cpp_dist/`，内容暂时为 placeholder，P3 再替换为真实翻译产出。

#### 4.2 新增 `native_test/` 源码生成

`family_root/native_test/test/native_test_main.cpp`:
```cpp
#include <cstdio>
int main() {
    printf("native test for convert-char\n");
    return 0;
}
```

`family_root/native_test/benchmark/native_benchmark_main.cpp`:
```cpp
#include <cstdio>
int main() {
    printf("native benchmark for convert-char\n");
    return 0;
}
```

`family_root/native_test/hotupdate/native_hotupdate_main.cpp`:
```cpp
#include <cstdio>
int main() {
    printf("native hotupdate for convert-char\n");
    return 0;
}
```

#### 4.3 新增 `solution_native/` cmake 生成

`solution_native/CMakeLists.txt`（根 cmake）:
```cmake
cmake_minimum_required(VERSION 3.20)
project(FoundationDllNative VERSION 1.0 LANGUAGES CXX)
add_subdirectory(System.Private.CoreLib/convert-char)
```

`solution_native/System.Private.CoreLib/convert-char/CMakeLists.txt`:
```cmake
add_executable(convert_char_test
    ../../../System.Private.CoreLib/convert-char/native_test/test/native_test_main.cpp
)
add_executable(convert_char_benchmark
    ../../../System.Private.CoreLib/convert-char/native_test/benchmark/native_benchmark_main.cpp
)
add_executable(convert_char_hotupdate
    ../../../System.Private.CoreLib/convert-char/native_test/hotupdate/native_hotupdate_main.cpp
)
```

#### 4.4 删除的生成

- 不再生成 `benchmark/` 下的 ManagedBenchmarkHarness.cs/.csproj/BenchmarkManagedBody.cs（managed benchmark 由 test_code_generator 通过 `managed_test/benchmarks/` 处理）
- 不再生成 `host/` 下的 hotupdate 独立文件

#### 4.5 `generate_benchmark_managed_bodies()` 保留但输出路径改为 `managed_test/benchmarks/`

实际 managed benchmark 已由 test_code_generator 通过 benchmarks exe 源码处理，此函数可简化或移除。P1 中先保留签名但不输出（或输出少量辅助文件到 `managed_test/benchmarks/`）。

## 调用链影响

### `gap_analyzer.py`

`_analyze_payload()` 中的调用顺序：
1. `test_code_generator_module.generate_family_skeleton()` — 签名不变，artifacts 路径更新
2. `truth_skeleton_generator_module.generate_truth_skeleton()` — 不改
3. `review_bundle_generator_module.generate_review_bundle()` — 不改
4. `native_codegen_generator_module.generate_native_skeleton()` — 签名不变，输出到 il2cpp_dist/
5. `native_codegen_generator_module.generate_benchmark_managed_bodies()` — 可简化或移除
6. `native_codegen_generator_module.generate_benchmark_native_entry()` — 输出路径改为 native_test/
7. `execution_project_generator_module.generate_execution_projects()` — 签名不变，输出到 solution/
8. `execution_solution_generator_module.generate_execution_solution()` — solution_path 改为 solution/

**不需要改 gap_analyzer.py 的调用逻辑**，只改 execution_solution_generator 的 solution_path 参数。

### `run_phase2.py`

只引用 `test_code_generator` 的工具函数（`_parse_method_subject_id` 等），不影响。这些工具函数不改变。

## 验证方式

### 验证步骤

1. 手动运行 gap_analyzer 的 auto_generate（或直接调用各 generator 函数）
2. 检查 convert-char 家族的新目录结构是否完整
3. `dotnet build solution/FoundationDllTranslationSolution.sln` 0 error

### 验证命令

```bash
# 1. 生成
cd D:/agent/booming-il2cpp
python -c "
from pathlib import Path
from build.toolchains.run.testing.foundation_dll.gap_analyzer import analyze_gaps
result = analyze_gaps(Path('.'), scope='all', auto_generate=True)
print('generatedArtifacts:', result['generatedArtifacts'])
"

# 2. 验证目录结构
ls verification/foundation-dll/System.Private.CoreLib/convert-char/src/
ls verification/foundation-dll/System.Private.CoreLib/convert-char/src/patch/
ls verification/foundation-dll/System.Private.CoreLib/convert-char/managed_test/tests/
ls verification/foundation-dll/System.Private.CoreLib/convert-char/managed_test/benchmarks/
ls solution/System.Private.CoreLib/convert-char/

# 3. Build
dotnet build solution/FoundationDllTranslationSolution.sln
```

## 执行步骤

### Step 1: 修改 `test_code_generator.py`

- 修改 `generate_family_skeleton()` 中的路径
- 修改 `_handwritten_source()` 内容
- 修改 `_generated_source()` 合并 benchmark/host/test
- 新增 `_patch_handwritten_source()` 
- 新增 test exe 源码生成模板
- 新增 benchmark exe 源码生成模板
- 删除旧的分文件生成逻辑（benchmark/host 独立文件）

### Step 2: 修改 `execution_project_generator.py`

- 添加 `_src_project_template()`、`_patch_project_template()`、`_tests_project_template()`、`_benchmarks_project_template()`
- 修改 `generate_execution_projects()` 输出路径和逻辑
- 删除旧的 5-project 逻辑

### Step 3: 修改 `execution_solution_generator.py`

- 修改 `generate_execution_solution()` 输出到 `solution/`
- 添加 solution folder 支持
- 更新 `gap_analyzer.py` 中的 solution_path

### Step 4: 修改 `native_codegen_generator.py`

- `generate_native_skeleton()` 输出到 `il2cpp_dist/`
- 新增 native_test C++ 源码生成函数
- 新增 solution_native/ cmake 生成函数
- `generate_benchmark_native_entry()` 输出路径调整

### Step 5: 验证

- 运行 generator，检查目录结构
- `dotnet build` 验证

## 不需要做的事

- 不改 `gap_analyzer.py` 调用逻辑（只改 solution_path 参数）
- 不改 `run_phase2.py`
- 不改现有单元测试（后续 P2 migrate-files 时统一更新）
- 不改 `_parse_method_subject_id`、`_family_slug`、`_class_name`、`_enum_name` 等工具函数
- 不移除旧目录文件（P2 负责）
