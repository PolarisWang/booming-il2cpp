## 2026-04-16 00:31:55 +08:00

- 用 `ManagedClosureContracts.cs`、`capability_coverage.py`、`release_evidence_contracts.py` 把 deferred surface 的 capability ledger 和 release evidence contract 固化下来。
- `declared_metadata_labels.py`、`registry.py`、`subject_reporting.py` 已接到 capability contract，而不是继续散落在 subject 特例逻辑里。
- Phase 0 对应回归已经包含在 compatibility / reporting / registry 总体验证内。
