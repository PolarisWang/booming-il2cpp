# Progress 2026-04-11 19:59:47 +08:00

## 本轮完成

- 新增 `subjects/CompatibilityMatrixProof/compatibility-matrix.json`，固定 Batch 1 的配置格式：
  - `aotVersions`
  - `hotUpdateVersions`
  - `platforms`
  - `additionalTestCases`
- 新增 `subjects/CompatibilityMatrixProof/` proof subject：
  - `subject.manifest.json`
  - `source/CompatibilityMatrixProof.csproj`
  - `source/Program.cs`
- 新增 `build/toolchains/run/testing/compatibility_matrix_runner.py`：
  - build proof project
  - 执行 compatibility matrix config
  - 写出 JSON report / stdout / stderr artifact
- `CompatibilityMatrixProof` 当前以 `1.0.0 / 1.0.1 / 1.0.2` patch-level AOT 版本样例对齐已落地的 `major.minor` compatibility band 规则；额外 negative case 使用 `2.0.0` 触发 reject。

## 验证

- `python -m pytest tests/unit/run/test_phase8_productization_gates.py -v`
- `python -m pytest tests/unit/run/test_subject_manifest_schema.py -v`
- `dotnet run --project subjects/CompatibilityMatrixProof/source/CompatibilityMatrixProof.csproj`

## 下一步

- 转向 Batch 4，优先补 perf regression dashboard 配置与 unsupported feature report skeleton。
- soak harness 若继续推进，需要先确认现有 perf harness 是否足以复用成长期运行入口。
