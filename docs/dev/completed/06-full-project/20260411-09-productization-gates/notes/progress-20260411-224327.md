# Progress 2026-04-11 22:43:27 +08:00

## 本轮完成

- 修复 perf goal 默认选择错误。
  - `build/toolchains/run/testing/subject_planner.py` 改为先按 `goal` / `matrix` 选中目标 perf matrix，再把 `validationMode=perf` 传给 validation 解析。
  - `build/toolchains/run/testing/subjects.py` 新增 goal-aware perf profile 选择逻辑；当命令只传 `--goal perf.*` 时，会自动落到对应的 `perf-*` profile，并把 `validationKind` 推导为 `perf`、`variant` 推导为 `PROFILE`。
- 修复 perf baseline 比对静默误判。
  - `build/toolchains/run/testing/perf.py` 现在会把“baseline 指标在实际 evidence 中缺失”视为回归，返回 `regressed`，不再把零交集错误地记成 `ok`。
- 补齐回归测试并验证真实链路。
  - 新增 planner / perf policy / command 三类测试，覆盖 `InterfaceDispatchProof` 与 `GenericEcho` 的 perf 默认选择，以及 baseline 指标缺失必须失败的策略。
  - 真实复跑四个 perf dashboard 条目：`GenericEcho` (`perf.dev` / `perf.release`)、`InterfaceDispatchProof` (`perf.profile`)、`MainlineFeaturePack` (`perf.profile`)。

## 验证

- `python -m pytest tests/unit/run/test_subject_planner.py tests/unit/run/test_phase2_perf_governance.py tests/unit/run/test_subject_perf_policy.py tests/unit/run/test_subject_workers_perf.py tests/tooling/run/test_subject_command.py tests/unit/run/test_phase8_productization_gates.py -v`
- `python -m pytest tests/unit/run tests/tooling/run -q`
- `python build/toolchains/run/run.py test subject --id subject/InterfaceDispatchProof --goal perf.profile`
- `python build/toolchains/run/run.py test subject --id subject/MainlineFeaturePack --goal perf.profile`
- `python build/toolchains/run/run.py test subject --id subject/GenericEcho --goal perf.dev`
- `python build/toolchains/run/run.py test subject --id subject/GenericEcho --goal perf.release`

## 结论

- `InterfaceDispatchProof` 的真实 perf run 已不再错误落到 `proof-dev` / `CHECK`，而是正确落到 `perf-profile` / `perf` / `PROFILE`，并在 `stdout.log` 与 `perf.runtime.json` 中产出 `dispatchNanoseconds` 指标。
- `Batch 5.3 perf baseline 无回归` 的本地判定逻辑已从“可能静默误报通过”收敛到“profile/variant 正确，baseline 指标缺失会显式回归”。
- 当前剩余主阻塞已经收敛为外部环境项：Android `24h soak` 运行窗口，以及 macOS/Xcode 条件下的 iOS runtime evidence。

## 产物

- `artifacts/productization-gates/20260411-224327/perf-dashboard.json`
- `artifacts/productization-gates/20260411-224327/perf-runs-summary.json`
