# Progress 2026-04-11 22:52:01 +08:00

## 本轮完成

- 对 Android runtime 主线做了一次真实 soak pilot。
  - 使用 `build/toolchains/run/testing/soak_harness.py` 包裹 `python build/toolchains/run/run.py test subject --id subject/MobileHelloWorldProof --goal correctness.platform --matrix windows-android-runtime`
  - 成功产出 `artifacts/productization-gates/20260411-225121/android-soak-pilot.json`
  - 单次真实 Android run 耗时约 `40.594s`，采到 `18` 个样本，`0 crash`
- 顺手验证了当前 soak 语义边界。
  - `subjects/MobileHelloWorldProof/validation/mobile/android-host/mobile_runtime_main.cpp` 只调用一次 `il2cpp_host_run()`
  - `src/mobile/shared/host_main.cpp` 当前只输出一次 host result 后退出
  - 现有 `soak_harness.py` 也是“循环执行命令”的模型，而不是天然面向单个长生命周期 mobile runtime session

## 结论

- 现有 harness 已经足够证明“可以包住一次真实 Android runtime command 并落盘报告”，所以不是完全不可用。
- 但它还不能直接证明 `Batch 4.3 / 5.2` 需要的“连续 4h / 24h mobile soak”。
- 当前剩余阻塞不再只是时间窗口，还包括一个能力边界：
  - 需要先明确并落地可信的 Android 长跑 soak entry/driver
  - 否则会把几十秒短命 command 误当成连续 soak，证据说服力不足

## 产物

- `artifacts/productization-gates/20260411-225121/android-soak-pilot.json`
