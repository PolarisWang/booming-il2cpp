# Progress 2026-04-11 21:04:03 +08:00

## 本轮完成

- 新增 `prepare-android-host` 与 `clean-android-host` scope，把 Android SDK / NDK / adb / emulator 与 JDK 缓存引入统一入口。
- `build/toolchains/run/core/tooling.py` 新增 Android repo-cache bootstrap、环境注入与下载兼容性修复：
  - Adoptium 下载请求显式携带 `User-Agent`，规避默认 `urllib` 请求被 403 拒绝。
  - Android overrides 改为“repo 路径前置 + 保留系统 PATH”，避免 `sdkmanager.bat` 因找不到 `findstr` 误报 Java 版本不足。
- `doctor` 现在会优先识别显式环境变量，其次识别完整的 repo cache；Windows 平台不再输出 `ios-runtime-host`，iOS host 仅在 macOS 上验证。
- 入口菜单与 manifest 已接入 Android host 准备/清理能力，Windows prepare submenu 可直接触发 `android-host`。
- 当前 Windows 主机已真实执行：
  - `python build/toolchains/run/run.py clean --scope android-host`
  - `python build/toolchains/run/run.py prepare android-host`
  - `python build/toolchains/run/run.py doctor`
  - `artifacts/toolchains/android/jdk/bin/java.exe -version`
  - `artifacts/toolchains/android/sdk/platform-tools/adb.exe version`
  - `artifacts/toolchains/android/sdk/emulator/emulator.exe -version`

## 验证

- `python -m pytest tests/tooling/run/test_doctor.py tests/tooling/run/test_prepare_scopes.py tests/tooling/run/test_command_manifest.py tests/tooling/run/test_tui.py tests/tooling/run/test_android_bootstrap.py -v`
- `python -m pytest tests/tooling/run -v`
- `python build/toolchains/run/run.py clean --scope android-host`
- `python build/toolchains/run/run.py prepare android-host`
- `python build/toolchains/run/run.py doctor`
- `artifacts/toolchains/android/jdk/bin/java.exe -version`
- `artifacts/toolchains/android/sdk/platform-tools/adb.exe version`
- `artifacts/toolchains/android/sdk/emulator/emulator.exe -version`

## 下一步

- 继续推进真实 Android emulator / device 证据采集，把已恢复的 host tooling 接到 24h soak、perf dashboard evidence 与 full compatibility matrix 复跑。
- iOS 相关 runtime evidence 仍只在 macOS 主机验证，Windows 平台保持跳过。
