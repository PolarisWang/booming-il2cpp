# Progress 2026-04-11 17:26:53 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 proof 升级为要求真实外部 `call` lowering 与真实 lowered interpreter -> AOT route。
- 为 `subjects/InterpreterArithmeticProof` 增加独立子项目 `source/AotBridge/InterpreterArithmeticProof.AotBridge.csproj` 与 `AotBridgeExports.Add(int,int)`，用外部程序集引用稳定制造真实 bridge call metadata。
- 扩展 `src/managed/Chaos.IL2CPP.Interpreter/ILToIRLowering.cs`，新增 `call` / `callvirt` lowering，并把外部程序集方法引用降到 `IROpCode.CallBridge`。
- 扩展 `subjects/InterpreterLoweringProof/source/Program.cs`，新增 `call-method-subject`、`call-ops`、`call-target` 输出，固定真实 callbridge lowering proof。
- 扩展 `subjects/MixedExecutionProof/source/Program.cs`，不再使用纯手写 `CreateCallBridgeMethod()`，而是从 hot-update assembly 里真实 lowering `CallAotBridgeAdd` 并执行，输出 `mixed-interpreter-to-aot-ops` 与 `mixed-interpreter-to-aot-target`。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -k "real_lowering_proof_subject_executes_add_method_into_ir or mixed_execution_proof_runs_aot_to_interpreter_and_interpreter_to_aot_paths" -v`
- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前真实 lowering 只证明外部静态 `call -> CallBridge`；同程序集 `Call` 执行还没打通。
- `callvirt` 目前只在 lowering 层进入最小实现，缺少对应 proof subject。
- 解释器异常 proof 仍只覆盖 `Throw -> AOT catch`，不代表内部 EH dispatcher 已完成。

## 下一步

- 优先进入解释器内部 `Call` 执行路径；如复杂度超预期，则退一步先补真实 `callvirt` lowering proof。
