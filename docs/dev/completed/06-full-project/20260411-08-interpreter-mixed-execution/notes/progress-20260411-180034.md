# Progress 2026-04-11 18:00:34 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 proof 提升为要求 `leave/finally` 最小执行路径与字符串桥执行路径。
- 在 `src/managed/Chaos.IL2CPP.Interpreter/ManagedInterpreterExecutor.cs` 新增 `IROpCode.Leave` / `IROpCode.EndFinally` 最小控制流语义，并把值槽从纯 `int` 扩展到最小 `object?`，补齐 `IROpCode.LdStr` / `IROpCode.LdNull` 与 `CallBridge` 的非 `int` 参数通路。
- 在 `subjects/MixedExecutionProof/source/Program.cs` 新增：
  - `mixed-interpreter-leave-finally=ok`
  - `mixed-interpreter-string-bridge=5`
  - `mixed-interpreter-string-bridge-ops=ldstr,callbridge,ret`
- 在本轮探索中验证了一个重要边界：当前 `CallStringLength()` 的 lowering 虽然能指向 `System.String::get_Length()`，但这不足以宣称“真实实例 receiver 计数 / callvirt execution 已闭环”；因此本轮刻意把执行 proof 收敛为更准确的 `ldstr + callbridge` 字符串桥执行证明。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前 EH 仍只有 `Throw -> AOT catch` 与手写 IR 的 `leave/finally` 清理 proof，尚未形成 interpreter 内部 `try/catch/finally` dispatcher。
- 当前字符串桥执行 proof 证明的是 `ldstr + callbridge` 路径，不代表真实实例 receiver 计数已经在 lowering 闭环。
- core solution 构建仍存在既有 warning：
  - `src/managed/Chaos.IL2CPP.Loader/LoaderStage.cs(153,13): CS1717`
  - 多处既有 nullable warning 位于 `Chaos.IL2CPP.CodeGen` 与 `Chaos.IL2CPP.Driver`

## 下一步

- 优先评估 `rethrow` 最小 proof；若当前 IR 缺少可验证的异常状态承载，则切换到实例 receiver / instance call lowering 专题。
