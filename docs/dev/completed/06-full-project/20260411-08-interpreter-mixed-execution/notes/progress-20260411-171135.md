# Progress 2026-04-11 17:11:35 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 `MixedExecutionProof` 的期望输出补到 `mixed-interpreter-throw-caught=ok`，明确锁定 `interpreter throw -> AOT catch` 的最小异常边界证据。
- 扩展 `src/managed/Chaos.IL2CPP.Interpreter/ManagedInterpreterExecutor.cs`，新增 `IROpCode.Throw` 的最小执行语义；当前用 `InvalidOperationException` 承载 proof 级异常跨边界传播。
- 扩展 `subjects/MixedExecutionProof/source/Program.cs`，新增 `RunInterpreterThrowCatchProof()` 与 `CreateThrowMethod()`，把 interpreter 抛错并由 AOT catch 的路径沉淀为固定 proof 输出。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -k mixed_execution_proof_runs_aot_to_interpreter_and_interpreter_to_aot_paths -v`
- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前异常 proof 只覆盖 `Throw -> AOT catch`，并不代表 interpreter 内部 EH dispatcher 已可用。
- `LoaderStage` 虽然已能 decode `call` / `callvirt` / `throw`，但 `ILToIRLowering` 仍未把真实方法调用降到 `IROpCode.Call` / `IROpCode.CallBridge`。
- `MixedExecutionProof` 中 interpreter -> AOT / engine 路径仍部分依赖手写 IR，真实 IL 驱动闭环还没完成。

## 下一步

- 优先进入 Batch 2.4 / 4.2：把真实 `call/callvirt` lowering 到 `IROpCode.Call` / `IROpCode.CallBridge`，并把 mixed execution proof 中的最小跨模式调用尽量替换为真实 IL 输入。
