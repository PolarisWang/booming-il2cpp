# Progress 2026-04-11 16:46:11 +08:00

## 本轮完成

- 补齐 `src/managed/Chaos.IL2CPP.Interpreter/ILToIRLowering.cs`，把 `ManagedMethodModel.Body` 降到 `InterpreterIR`，当前 baseline 覆盖 `ldarg`、`ldloc`、`stloc`、`ldc.i4`、`add/sub/mul/div/rem/ceq/clt/cgt`、`br/brtrue/brfalse` 与 `ret`。
- 扩展 `src/managed/Chaos.IL2CPP.Loader/LoaderStage.cs`，增加算术/比较 opcode decode，给 lowering 提供最小输入面。
- 新增 `subjects/InterpreterArithmeticProof` 与 `subjects/InterpreterLoweringProof`，形成真实 managed lowering proof：`InterpreterArithmeticProof/NativeExports::Add(System.Int32,System.Int32)` -> `ops=ldarg,ldarg,add,ret`。
- 新增 native `src/native/interpreter/` 模块、`chaos_interpreter` target，以及 `tests/contracts/native/interpreter/chaos_interpreter_smoke`；当前 smoke path 已覆盖 `ldarg,stloc,ldloc,ldarg,add,ret -> 42` 与 `ceq + brtrue -> 42`。
- Phase 7 定向测试由 5 项扩到 8 项，managed/native proof 全部纳入同一入口。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`
- `cmake -S . -B artifacts/.tmp-interpreter-native -G "Visual Studio 17 2022" -DROADMAP0_PRESET_TARGET=windows-x64-reference`
- `cmake --build artifacts/.tmp-interpreter-native --config Release --target chaos_interpreter`

## 当前风险

- Loader 仍未覆盖完整 IL opcode；真实 lowering proof 现在必须继续走 dedicated minimal input assembly。
- Windows 下 subject proof 容易受 `.NET Host` 文件锁污染；测试已通过唯一 `BaseOutputPath` 规避，但后续 subject proof 也应复用这一策略。
- native `InterpreterVM` 目前已覆盖整数常量 / 参数 / 局部变量 / 加减乘除余 / 比较 / 基本分支 / 返回，但仍不代表 mixed execution 已经可用。

## 下一步

- 优先进入 Batch 4 mixed execution，补 AOT -> interpreter execute 与 `ir_call_bridge` 的最小跨模式调用路径。
