# Progress 2026-04-11 17:37:14 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 lowering proof 升级为要求真实 `callvirt` 输入。
- 为 `subjects/InterpreterArithmeticProof` 新增 `CallStringLength()`，用 `ldstr + callvirt + ret` 构造最小真实 `callvirt` IL。
- 扩展 `subjects/InterpreterLoweringProof/source/Program.cs`，新增 `callvirt-method-subject`、`callvirt-ops`、`callvirt-target` 输出。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -k real_lowering_proof_subject_executes_add_method_into_ir -v`
- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前 `callvirt` 只覆盖 lowering proof，不覆盖真实对象/接口执行。
- EH 路径仍只有 `Throw -> AOT catch` 最小证据。
- 更复杂的对象/接口/泛型调用仍未进入执行层 proof。

## 下一步

- 优先进入 EH 的 `leave` / `rethrow` / `endfinally` 最小 proof。
