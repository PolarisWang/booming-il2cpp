# 2026-04-17 13:03 Progress

## 本次推进

- 激活 `20260417-03-phase-0-full-rebirth-authority-freeze`
- 完成 Phase 0 authority audit
- 新增 3 个 canonical subject 的 `subject.features.json`
- 新增 contract test `tests/contracts/shared/test_subject_authority_freeze.py`
- 将父任务执行入口切到 Phase 0 子任务

## 关键发现

- `subject.manifest.json` 仍然过载，已经不是单纯的 subject/source/pipeline 配置
- collection 仍未 versioned，且 Runtime/Python 的消费面不一致
- capability coverage 仍由 Python 硬编码中心维护
- tests 对 `Archetypes` / `FeatureSlices` / 真实 subject 布局耦合很重

## 对后续的直接影响

- Phase 1 不能先做“局部 generator 优化”，必须先统一 collection 与 features authority
- Phase 2 之前，dashboard/registry 不能再继续新增旧 manifest 字段依赖
