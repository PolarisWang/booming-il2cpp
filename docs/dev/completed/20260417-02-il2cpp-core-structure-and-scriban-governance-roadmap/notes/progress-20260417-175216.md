# 2026-04-17 17:52 Progress

## 当前动作

- 正式进入 `20260417-02` 的执行阶段。
- 当前采用“先补结构治理回归测试，再做首批无行为迁移”的策略。

## 当前锁定的首批切口

1. `NativeReferenceLoweringPlanner` 与 `NativeReferenceProofEmitter` 的共享 family/template catalog。
2. `NativeAotEmitter` 与 `NativeReference*` 的共享 `Scriban` 渲染基础设施。
3. `NativeAotLoweringPlanner` runtime prelude 族从主文件拆出。

## 原因

- 这三项都能直接减少重复职责。
- 都可以通过文本级 compatibility 测试稳定锁定。
- 都不要求先改 subject/test 主线，不会把风险扩散到其它链路。

## 下一步

- 新增结构治理回归测试。
- 跑红灯，确认当前实现尚未满足这些约束。
