# 2026-04-17 10:32 Progress

## 本轮完成

- 修复 `NativeReferenceProofEmitter` 共享 Scriban renderer 接线后的结构回归测试。
- 把一批 `compatibility` 测试从“原文件里必须直接出现 raw lowering family/template path 字面量”调整为“catalog / shared renderer / partial file 才是结构真相源”。
- 新增并跑通 `NativeAotLoweringPlanner` 结构治理回归：
  - `RuntimeSupport/NativeAotLoweringPlanner.RuntimePrelude.cs`
  - `Planning/NativeAotLoweringPlanner.MetadataSupport.cs`
  - `Planning/NativeAotLoweringPlanner.InvocationPlanning.cs`
- 用连续区段搬移的方式把以下逻辑从 `NativeAotLoweringPlanner.cs` 主文件拆出：
  - `EmitRuntimePrelude` / `EmitSpanRuntimePrelude`
  - `BuildCustomAttributeSupportModel` / `BuildAssemblyReflectionSupportModel` / `BuildReflectionMemberSupportModel` / `BuildStaticFieldDataSupportModel`
  - `CollectReachableMethods` 到 `TryGetLowerableMethod` 的 invocation/reachability 解析段
- 修正 `NativeReference` catalog alias 在 `switch case` 场景下的编译期常量要求，改为 `const alias -> catalog const`，避免回退到 duplicated raw string constants。

## 验证

- `python -m pytest tests/unit/compatibility/test_il2cpp_codegen_structure_governance.py tests/unit/compatibility/test_phase4b_aot_core_ir_object_model.py tests/unit/compatibility/test_capability_driven_lowering.py tests/unit/compatibility/test_async_await_proof_subject.py tests/unit/compatibility/test_engine_host_proof_subject.py tests/unit/compatibility/test_engine_binding_subjects.py tests/unit/compatibility/test_mainline_capability_batch_a.py tests/unit/compatibility/test_mainline_capability_batch_b.py tests/unit/compatibility/test_marshaling_proof_subject.py tests/unit/compatibility/test_nested_exception_proof_subject.py tests/unit/compatibility/test_threading_gc_proof_subject.py -q`
  - 36 passed
- `dotnet build src/managed/Chaos.IL2CPP.CodeGen/Chaos.IL2CPP.CodeGen.csproj -c Release -m:1`
  - succeeded

## 当前判断

- `NativeAotLoweringPlanner.cs` 已继续缩小，但核心热点仍然在：
  - method emission
  - object model emission
  - external runtime helper generation
- 下一轮优先继续把上述三块往 `Emission/` 和 `RuntimeSupport/` 收口，而不是去碰更外围的 loader/linker stage。
