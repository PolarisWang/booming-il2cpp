# 2026-04-17 06:35

## 本轮进展

- 修复了 `PerfHarness` 真实 hotupdate benchmark 路径：
  - collection-backed benchmark descriptor 改为直接读取 `declared-tests.collection.json`
  - hotupdate benchmark host 优先走生成的 `InvokeWorkload(...)`
  - 反射 benchmark host 时不再依赖扫描 host `Entries` / `Execute` 或转调 `Main(string[] args)`
- 修复了 `ChaosHotUpdateExecutor` 在 harness 进程内的依赖解析：
  - 之前 `HotUpdateHostPack.dll` 在反射执行时会继续按 `AppContext.BaseDirectory` 解析二级依赖，导致找不到 `Chaos.IL2CPP.HotUpdate.dll`
  - 现在通过“当前调用基目录”把 resolver 对齐到 host 输出目录
- 重新生成了 `HotUpdateHostPack` workspace，使真实 `ChaosGeneratedHotUpdateBenchmarkHost.g.cs` 落到新的 `InvokeWorkload + ResolveHostBaseDirectory()` 合约
- 顺手开始清理统一入口的旧术语：
  - `build/toolchains/run/commands/test.py` 的 workspace declared `catalog` 用户提示已改为 `collection`

## 验证

- `python -m pytest tests/integration/run/test_perf_harness_hotupdate_benchmark_host.py -q`
  - `1 passed`
- `python -m pytest tests/unit/planning/test_generated_hotupdate_hosts.py tests/unit/execution/test_subject_workers.py tests/unit/performance/test_subject_workers_perf.py -q`
  - `46 passed`
- `python -m pytest tests/tooling/run/test_subject_command.py tests/unit/execution/test_subject_workers.py tests/unit/planning/test_generated_hotupdate_hosts.py tests/integration/run/test_perf_harness_hotupdate_benchmark_host.py tests/unit/compatibility/test_phase5_hotupdate_host_pack_declared_proof.py -q`
  - `54 passed`
- `python -u build/toolchains/run/run.py test declared-benchmark --id "declared-benchmark/HotUpdateHostPack::HotUpdateHostPack::HotUpdateHostPack.HotUpdateLoadBenchmarkEntry::RunWorkload()" --json`
  - `final-summary.status = ok`
- `python -u build/toolchains/run/run.py test declared-unit-test --id "declared-unit-test/HotUpdateHostPack::HotUpdateHostPack::HotUpdateHostPack.HotUpdateSkeletonProofEntry::Run()" --json`
  - `final-summary.status = ok`

## 未收口项

- Phase 7 还需要继续清掉剩余 `compiled_catalog` / `workspace catalog` 命名和旧双轨逻辑
- `SolutionCorePack` 的 AOT native proof emission 阻塞仍在独立主线，不在本任务内展开实现
