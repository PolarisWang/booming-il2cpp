# 2026-04-17 03:10

## 本轮进展

- 将 workspace managed test contract 从 `catalogPath` 统一切到 `collectionPath`
- 将真实 collection 文件名统一切到 `declared-tests.collection.json`
- 把 declared benchmark 的 workspace host-input 构建切到 benchmark host
- 为 benchmark host 生成物补齐 `AssemblyName` / `DeclaringType` / `MethodName` / `MethodSignature`
- 扩展 `Benchmark.WorkloadEntry.PerfHarness`，支持 `--host-assembly + --collection-path + --entry-index`
- 让 managed / interpreter perf 都能消费 workspace benchmark host

## 验证

- `python -m pytest tests/unit/planning/test_project_workspace.py tests/unit/planning/test_generated_managed_hosts.py tests/unit/execution/test_subject_workers.py tests/unit/performance/test_workspace_declared_catalog.py tests/unit/performance/test_benchmark_command.py tests/unit/performance/test_benchmark_dashboard_generator.py tests/tooling/run/test_subject_command.py tests/integration/registry/test_declared_metadata_discovery.py -q`
  - `114 passed`
- `dotnet build src/validation/perf/Benchmark.WorkloadEntry.PerfHarness/Benchmark.WorkloadEntry.PerfHarness.csproj -c Release -m:1`
  - `Build succeeded`

## 未收口项

- native test host 仍未切到 `collection + dispatch manifest`
- hotupdate test host 仍未切到 `collection + binding manifest`
- `SolutionCorePack` 全量 workspace regenerate 仍被 AOT native proof emission 阻塞
