# Progress 2026-04-17 15:18:40 +08:00

## 本轮完成

- `subject_workers.py`
  - `benchmark.dispatch.manifest.json` 不再写入 `workloadEntry`
  - native AOT build 路径不再为 dispatch manifest 额外计算 `workloadEntry`
- 测试
  - 更新 `tests/unit/execution/test_subject_workers.py` 的 native AOT build contract
  - `tests/unit/execution/test_subject_workers.py -q` 通过
  - 扩展 Phase 2 回归 `167 passed`

## 影响

- structured benchmark host 与 native dispatch manifest 都已退出 `workloadEntry` 业务路由面
- `workloadEntry` 现在主要残留在非结构化反射 fallback 与 runtime/result evidence 字段

## 下一步

- 审计非结构化反射 fallback 是否可以完全依赖 declared metadata
- 区分 runtime/result manifest 里的 `workloadEntry` 哪些只是 trace-only 字段
