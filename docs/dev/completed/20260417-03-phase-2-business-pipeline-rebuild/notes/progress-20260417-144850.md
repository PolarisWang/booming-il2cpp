# 2026-04-17 14:48 Progress

## 本次推进

- 补了 `Phase 2` 第一批 RED tests，锁住 planner / registry / dashboard 三个 legacy manifest 业务输入面。
- 完成 `subject_planner.py` 切换：
  - `declared-benchmark` 在没有显式 `workload_entry` override 时，不再隐式读取 manifest `workloadEntry`
  - 同时不再把 manifest `source.entrySelection` 混进 declared benchmark 的默认选择
- 完成 `registry.py` 切换：
  - subject registry item 不再暴露 `displaySubjectEntrySelection`
  - subject registry item 不再暴露 `displayWorkloadEntry`
- 完成 `benchmark_dashboard_generator.py` 切换：
  - summary workload/case 改为优先从 declared benchmark case 解析
  - manifest `workloadEntry` 只保留兜底语义，不再直接驱动 dashboard summary

## 验证

- `python -m pytest tests/unit/planning/test_subject_planner.py tests/unit/registry/test_declared_registry_matrix_selection.py tests/integration/registry/test_registry_scan.py tests/unit/planning/test_project_workspace.py tests/unit/performance/test_benchmark_dashboard_generator.py tests/unit/performance/test_benchmark_command.py tests/tooling/run/test_subject_command.py -q`
  - `107 passed`

## 下一步判断

- `Phase 2` 剩余最明显的 legacy 业务依赖在 `project_workspace.py`：
  - 仍把 `subjects/*/validation/proof/native-reference/main.cpp` 当作 native proof workspace 可生成的前提
  - 这会阻碍后续 `Phase 3` 对 subject 目录结构的全量重生
- 下一刀要把 native proof workspace 改成依赖 workspace/generated native test host contract，而不是 subject 目录里的旧 proof 物料。
