# Progress 2026-04-17 15:06:51 +08:00

## 本轮完成

- `project_workspace.py`
  - 新增 `templates/native-proof-main.cpp.tmpl`
  - Windows workspace native proof host 改成模板生成
  - 移除对 `subjects/*/validation/proof/native-reference/main.cpp` 的正式依赖
- `perf_dashboard.py`
  - summary workload/case 改成 declared benchmark case 优先
  - stale manifest `workloadEntry` 不再成为 summary 展示真源
- 测试
  - 新增 `tests/unit/planning/test_project_workspace.py` 的 template proof host cutover 用例
  - 新增 `tests/unit/performance/test_perf_dashboard.py`
  - 交叉回归 `130 passed`

## 影响

- `Phase 2` 里最阻塞 `Phase 3 subject rebirth` 的 workspace legacy 依赖已切掉。
- reporting/dashboard 继续向 declared metadata + collection 收口，减少 manifest/string 误导展示。

## 下一步

- 审计 `subject_workers.py` / reporting 剩余 `workloadEntry` 用法
- 区分 display/evidence 字段与真实路由字段
- 继续补 RED tests 后再做执行链 cutover
