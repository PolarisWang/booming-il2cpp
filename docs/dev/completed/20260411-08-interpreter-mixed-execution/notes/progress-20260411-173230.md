# Progress 2026-04-11 17:32:30 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 proof 升级为要求同程序集 `Call` lowering 与最小执行证据。
- 为 `subjects/InterpreterArithmeticProof` 新增 `CallLocalAdd(int,int)`，用同程序集静态方法调用制造真实 `call` 输入。
- 扩展 `src/managed/Chaos.IL2CPP.Interpreter/ManagedInterpreterExecutor.cs`，新增 `IROpCode.Call` 与 `methodResolver`，使解释器可以按 subject id 解析同程序集 callee 并递归执行。
- 扩展 `subjects/InterpreterLoweringProof/source/Program.cs`，新增 `local-call-method-subject`、`local-call-ops`、`local-call-target` 输出。
- 扩展 `subjects/MixedExecutionProof/source/Program.cs`，新增 `mixed-interpreter-local-call=42` 与 `mixed-interpreter-local-call-ops=ldarg,ldarg,call,ret`，把同程序集方法互调沉淀为固定 proof。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -k "real_lowering_proof_subject_executes_add_method_into_ir or mixed_execution_proof_runs_aot_to_interpreter_and_interpreter_to_aot_paths" -v`
- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前同程序集 `Call` 只覆盖 `Int32 -> Int32` 的最小递归执行，不代表完整调用栈、实例方法、泛型方法或 `callvirt` 已闭环。
- `callvirt` 目前仍缺少对应 proof subject。
- 异常路径仍只有 `Throw -> AOT catch` 最小证据，内部 EH dispatcher 未完成。

## 下一步

- 优先进入真实 `callvirt` lowering proof；如对象/接口入口过早拉高复杂度，则回到异常路径推进 `leave` / `rethrow` / `endfinally`。
