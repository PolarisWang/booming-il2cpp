# Progress 2026-04-11 18:09:50 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 proof 提升为要求：
  - `InterpreterIR` 显式包含块级 `IRExceptionRegion` / `IRExceptionRegionKind`
  - `ManagedInterpreterExecutor` 显式支持 `IROpCode.Rethrow`
  - `MixedExecutionProof` 输出 `mixed-interpreter-rethrow-caught=ok`
- 在 `src/managed/Chaos.IL2CPP.Interpreter/InterpreterIR.cs` 新增块级异常区域元数据：
  - `IRExceptionRegion`
  - `IRExceptionRegionKind`
  - `IRMethod.ExceptionRegions`
- 在 `src/managed/Chaos.IL2CPP.Interpreter/ManagedInterpreterExecutor.cs` 新增：
  - 基于块级 `catch` 区域的最小异常跳转
  - `IROpCode.Rethrow` 最小语义
- 在 `subjects/MixedExecutionProof/source/Program.cs` 新增 `CreateRethrowMethod()` 与 `mixed-interpreter-rethrow-caught=ok`，形成 `Throw -> catch block -> Rethrow -> AOT catch` 的最小 proof。

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 当前 `rethrow` proof 依赖手写 IR 的块级 `catch` 区域元数据，不代表真实 `ManagedExceptionRegionModel -> IR EH` lowering 已闭环。
- 当前 EH 仍未形成完整 `try/catch/finally` dispatcher；`leave/finally` 与 `rethrow` 仍是分批最小 proof。
- 实例 receiver / instance call lowering 仍未打通；`callvirt-target` 目前仍只代表 lowering 边界。

## 下一步

- 优先转向实例 receiver / instance call lowering；若对象语义入口过早拉高复杂度，则回到真实 `ManagedExceptionRegionModel` -> IR EH lowering / dispatcher。
