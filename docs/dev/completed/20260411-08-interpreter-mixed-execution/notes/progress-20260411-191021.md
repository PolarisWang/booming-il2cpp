# Progress 2026-04-11 19:10:21 +08:00

## 本轮完成

- 基于 RED/GREEN 扩展 `tests/unit/run/test_phase7_interpreter_mixed_execution.py`，把 Phase 7 验收面提升为要求：
  - same-assembly `CallVirt` 真实执行 proof
  - 真实 `ManagedExceptionRegionModel -> IRExceptionRegion` EH lowering
  - 真实 `try/catch` / `throw;` / `try/finally` managed proof
- 在 `src/managed/Chaos.IL2CPP.Contracts/ManagedClosureContracts.cs` 为 `ManagedInstructionModel` 新增 `IlOffset`，让 loader 输出能保留真实 IL offset。
- 在 `src/managed/Chaos.IL2CPP.Loader/LoaderStage.cs`：
  - 为解码指令附带 `IlOffset`
  - 将 `br` / `brtrue` / `brfalse` / `leave` operand 从相对 delta 归一成绝对 IL target offset
  - 在 `SubstituteMethodBody()` 里保留 `IlOffset`
- 重写 `src/managed/Chaos.IL2CPP.Interpreter/ILToIRLowering.cs` 的 Phase 7 lowering 路径：
  - 基于 `IlOffset` 自行切 IR block，而不改动 loader 的单块 contract
  - 新增真实 `ExceptionRegions` lowering
  - 支持 `leave` / `endfinally` / `rethrow`
  - 为真实 catch handler 补齐最小 `pop` 兼容
- 在 `src/managed/Chaos.IL2CPP.Interpreter/ManagedInterpreterExecutor.cs`：
  - 扩展 object receiver 参数读取
  - 补齐 same-assembly `CallVirt` 执行
  - 保持最小 `Pop` 执行兼容
- 在 `subjects/InterpreterArithmeticProof/source/Program.cs` 新增：
  - `InstanceArithmetic.AddOne`
  - `CallInstanceAddOne`
  - `DivideOrCatch`
  - `DivideOrRethrow`
  - `AddWithFinally`
- 在 `subjects/InterpreterLoweringProof/source/Program.cs` 补齐 `instance-call-*` 输出。
- 在 `subjects/MixedExecutionProof/source/Program.cs` 补齐：
  - `mixed-interpreter-instance-call=*`
  - `mixed-interpreter-real-catch=*`
  - `mixed-interpreter-real-rethrow-*`
  - `mixed-interpreter-real-leave-finally-*`

## 验证

- `python -m pytest tests/unit/run/test_phase7_interpreter_mixed_execution.py -v`
- `python -m pytest tests/unit/run/test_phase6_capability_batch_b.py -v`
- `dotnet build src/managed/Chaos.IL2CPP.Interpreter/Chaos.IL2CPP.Interpreter.csproj -c Release`
- `dotnet build solutions/core/windows/chaos-il2cpp-core.sln -c Release`

## 当前风险

- 真实 EH lowering 当前只证明 minimal `catch` / `rethrow` / `finally` 主线；尚未扩展到 catch type filtering、filter/fault handler 或更宽的对象异常模型。
- 同程序集 `CallVirt` 已能跑通 receiver 透传，但并不代表完整虚派发、接口调用或 devirtualization 策略已完成。
- core solution 仍有既有 warning；本轮没有把这些 warning 当作 Phase 7 gating item。

## 下一步

- 归档 `20260411-08-interpreter-mixed-execution`，将父 roadmap 的剩余阻塞重新收敛到 Phase 4 mobile runtime host 环境证据。
