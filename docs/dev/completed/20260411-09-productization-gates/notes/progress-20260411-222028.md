# Progress 2026-04-11 22:20:28 +08:00

## 本轮完成

- 修复 `MobileHelloWorldProof` 在 `analysis-frontend` 的 lowering 分类失败。
  - 将 `subjects/MobileHelloWorldProof/source/Program.cs` 收敛到已支持的 captured-state instance-message 形状。
  - 新增回归测试，锁定移动 hello-world 入口必须保持在当前 lowering family 可识别的最小形状。
- 修复 Windows Android emulator host ABI 选择错误。
  - `build/toolchains/run/core/tooling.py` 改为按主机架构选择 Android emulator system image ABI。
  - 在当前 Windows x86_64 主机上，repo cache 改为安装 `system-images;android-36;google_apis;x86_64`，并生成 `chaos-android-36-x86_64` AVD。
  - `build/toolchains/run/commands/doctor.py` 同步按主机架构检查 system image / AVD，避免把不可启动的 `arm64-v8a` image 误判为可用。
- 真实链路验证已打通：
  - `clean --scope android-host` 后重新 `prepare android-host`
  - `doctor` 识别 repo-cache Android SDK / NDK / adb / emulator / x86_64 system image / AVD
  - `run.py test subject --id subject/MobileHelloWorldProof --goal correctness.platform --matrix windows-android-runtime` 实测通过

## 验证

- `python -m pytest tests/tooling/run/test_android_bootstrap.py tests/tooling/run/test_doctor.py tests/unit/run/test_phase4_mobile_hello_world_proof.py tests/unit/run/test_subject_workers.py -v`
- `python build/toolchains/run/run.py clean --scope android-host`
- `python build/toolchains/run/run.py prepare android-host`
- `python build/toolchains/run/run.py doctor`
- `python build/toolchains/run/run.py test subject --id subject/MobileHelloWorldProof --goal correctness.platform --matrix windows-android-runtime`
- `python -m pytest tests/tooling/run -v`
- `python -m pytest tests/unit/run/test_subject_manifest_schema.py tests/unit/run/test_subject_executor.py tests/unit/run/test_subject_planner.py tests/unit/run/test_subject_workers.py tests/unit/run/test_phase4_mobile_hello_world_proof.py tests/unit/run/test_phase8_productization_gates.py -v`

## 结论

- Windows 平台上的 Android host/runtime bootstrap 已不仅是“工具可发现”，而是能真实跑通 `MobileHelloWorldProof` 的 Android runtime observe 矩阵。
- 当前外部阻塞已从“Windows 缺 Android host/runtime 环境”进一步收敛到：
  - 更长时间的 Android perf / soak evidence
  - macOS 平台上的 iOS runtime evidence
  - full compatibility matrix / release checklist 最终复跑

## 备注

- 本轮确认的长期规则：Windows x86_64 主机不能直接使用 `arm64-v8a` Android emulator system image；repo-cache bootstrap 必须选择与主机匹配的 emulator image ABI。
