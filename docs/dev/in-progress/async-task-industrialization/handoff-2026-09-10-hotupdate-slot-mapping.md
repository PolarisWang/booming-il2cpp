# Handoff — async hotupdate `semanticChangedCount=0` 断点交接

> 交接入：async 工业化 agent
> 日期：2026-09-10
> 状态：根因已锁定，修复代码已写但需完整流水线验证

## 一句话 TL;DR

补丁 IL 已正确生成（`0xBEEF0000` sentinel），template 已插入激活代码，**但 `ApplyPatchFromMemoryEx` 返回 null**，导致补丁从未生效。

---

## 已确认的事实（不要重新调查）

| 项目 | 状态 |
|------|------|
| `patch.patchdata` 中 112 个方法 `body_size=7`（哨兵体） | ✅ 已验证 |
| `BuildAotCoreIrSection` 发哨兵 IR（`operand: -1091633152` = `0xBEEF0000`） | ✅ 已验证 |
| `il_changed` 检测 + Phase 3 条件化 | ✅ 已合入 main |
| oracle 哨兵范围已改无符号比较 | ✅ 已合入 main |
| **`ApplyPatchFromMemoryEx` 返回 null** | ✅ **cdb/stderr 双确认** |
| slot 53 flags = `0x00000000`（patch 从未设置 flags） | ✅ DISPATCH_DIAG 确认 |
| Subject_N 名字不存在于 AOT `HotpatchNameRegistry` | ✅ 已验证 |
| `kSubjectSlotMap[0]=53` 是 harness 调度的 flat index | ✅ 已验证 |
| `patch-host-arrays.cpp` 名字 (`AutoGenerated.System_Threading_Tasks_Parallel.System_Threading_Tasks_ParallelTests`) 可能不在 registry | ⚠️ 待确认 |

---

## 已提交到 `origin/main`

| 提交 | 内容 |
|------|------|
| `c0c5b2746` | UInt32.Parse ShapeRegistry + native `ChaosParseUInt32` |
| `73537248f` | CoreLib segfault 栈→堆修复（**3779/3779 passed**） |
| `3a27db13d` | sentinel IR 扩展到所有 patch 方法 + oracle 无符号比较 |
| `93b5f1eda` | keep-native 条件化 + hotupdate_probe + ATG 探针 + secondary patch |

## 工作区未提交改动

| 文件 | 内容 | 状态 |
|------|------|------|
| `src/native/runtime-core/chaos/hotpatch_dispatch.h` | **临时** DISPATCH_DIAG（`semanticChangedCount>0` 后必须移除） | ⚠️ 未提交 |
| `src/tools/Chaos.IL2CPP.Tools.TestProjectGenerator/Templates/TestProject.RuntimeEntry.cpp.scriban` | Subject slot 激活循环 + `#include <atomic>` | ✅ 逻辑正确 TPG 编译通过 |
| `src/tools/Chaos.IL2CPP.Tools.AutoTestGenerator/CSharpExpressionBuilder.cs` | 外部改动，不相关 | — |

**下次必须先提交这两个文件**，否则 DISPATCH_DIAG 会被模板还原丢失。

---

## 修复代码说明

### subject slot 激活（`runtime-entry.cpp.scriban`）

`ApplyHotpatchFromFile` 后新增的代码块：

```
// ── Activate the patch on the SUBJECT dispatch slots ────────────────
if (patch_ctx != nullptr) {
    auto* subject_entries = const_cast<HotpatchEntryV0*>(GetHotpatchEntries());
    for (int si = 0; si < kCount; si++) {
        int slot = kSubjectSlotMap[si];
        ...
        auto& atomic_flags = *reinterpret_cast<std::atomic<uint32_t>*>(&subject_entries[slot].flags);
        uint32_t new_flags = (atomic_flags.load(std::memory_order_relaxed)
                              | kHotpatchActive) & ~kHotpatchKeepNative;
        atomic_flags.store(new_flags, std::memory_order_release);
    }
}
```

### DISPATCH_DIAG（`hotpatch_dispatch.h`）

`ChaosDispatchMethodGetValue` 中条件性打印 flags、分支选择路径和返回值。通过 `CHAOS_DISPATCH_DIAG=1` 环境变量激活，**验证后必须移除**。

---

## 精确恢复入口

### 前提确认

```bash
git log --oneline -3       # 确认 HEAD 包含 3a27db13d
git diff --stat            # 确认工作区有 hotpatch_dispatch.h 和 template 改动
```

### 完整验证

```bash
cd D:/agent/chaos-il2cpp

# 1. 提交工作区改动
git add src/native/runtime-core/chaos/hotpatch_dispatch.h \
        src/tools/Chaos.IL2CPP.Tools.TestProjectGenerator/Templates/TestProject.RuntimeEntry.cpp.scriban
git commit -m "fix(hotupdate): subject slot 激活 + DISPATCH_DIAG 临时诊断"
git push origin main

# 2. 重建 preset lib（含 DISPATCH_DIAG + 新的 runtime 代码）
cmake --build artifacts/presets/windows-x64-reference --target chaos_runtime_core --config RelWithDebInfo

# 3. 跑完整流水线（含 patch data 生成）
timeout 3000 python -m testing.foundation-dll.verification.chunk_pipeline \
  --assembly System.Threading.Tasks.Parallel --chunk global-ns \
  --stages build,fact,hotupdate 2>&1 | tail -10

# 4. 用热更新模式跑诊断
cd testing/foundation-dll/System.Threading.Tasks.Parallel/chunks/global-ns/native
cp -p build/RelWithDebInfo/chaos_entry.exe entry.exe
CHAOS_DISPATCH_DIAG=1 ./entry.exe --hotupdate --patch-data patch.patchdata 2>&1 | grep -E "WARN|active=|interpreter ret"
```

### 预期结果（如果激活代码生效）

```
active=1   (flags 被改为 kHotpatchActive)
interpreter ret[0]=0xBEEFxxxx  (解释器返回哨兵值）
semanticChangedCount>0  （oracle 计数增长）
```

### 预期结果（如果 `ApplyPatchFromMemoryEx` 仍返回 null）

```
WARN: ApplyPatchFromMemoryEx returned null (no patches applied)
activePatchCount:0
active=1 count: 0
```

此时需要在 `patch_loader.cpp` 的 `ApplyPatchFromMemoryEx` 入口加诊断，打印第一个 `method_entry->body_size` 和 `LookupMethod` 返回值——最可能是 `patch-host-arrays.cpp` 的 host 名字在 AOT 的 `HotpatchNameRegistry` 中不存在。

---

## 关键代码索引

| 功能 | 文件 | 行号 |
|------|------|------|
| subject slot 激活代码 | `TestProject.RuntimeEntry.cpp.scriban` | ~847 |
| DISPATCH_DIAG | `chaos/hotpatch_dispatch.h` | ~246-277 |
| il_changed 检测 + Phase 3 | `patch_loader.cpp` | 802-820, 1155-1167 |
| sentinel IR 生成 | `PatchDataExtractor.Remap.cs` | 310-330 |
| oracle 哨兵范围检查 | `TestProject.RuntimeEntry.cpp.scriban` | ~907 |
| `ApplyPatchFromMemoryEx` 入口 | `patch_loader.cpp` | 899 |

---

## 风险提示

- **并行 agent（`booming-il2cpp`）** 在本次会话中至少 4 次通过 `ci-r7-clean` 整树 checkout 覆盖工作区。开工前先 `git log --oneline --since=2026-09-10 -- src/` 确认改动还在
- **DISPATCH_DIAG 是临时代码**，`semanticChangedCount>0` 确认后必须从 `hotpatch_dispatch.h` 移除，否则有潜在被合入上游的风险
- **`runtime-entry.cpp.scriban` 的激活代码** `#include <atomic>` 已添加，MSVC `/EHa` + `/GS-` 下验证编译通过
- 当前 chunk 构建有多个 `LNK4006` warning（multiple definition），这是已知问题（`/FORCE` link），不影响运行