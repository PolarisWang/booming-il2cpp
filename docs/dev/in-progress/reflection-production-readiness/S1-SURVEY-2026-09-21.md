# S1 普查：reflection chunk 390 stubGap receiver 构成（wf_ef9be6f5-cb1）

## R3-customattribute

R3-customattribute 族共 107 个 stubGap subject（CustomAttributeExtensions 78 + RuntimeReflectionExtensions 22 + CustomAttributeData 7），全部为 value=42/caught=false 的纯字面 stub（AOT body 完全缺失，非 receiver 运行期崩溃）。按 receiver 供给模式分六类：(A) 51 项 Set0 全 default——这是 ATG 有意生成的负向测试（期望 ArgumentNullException），非缺陷；(B) 16 项 receiver 仍为 default(...)! —— 真缺陷，且关键发现：MemberInfo/ParameterInfo/Type 三个类型**早已在** ReflectionInstanceFactories 表中有条目，说明这是参数集路由/顺序缺陷（default/boundary 集绕过 smart 集），修复成本极低、可直接回收 16 项；(C) 7 项 receiver 真实但第二参 default(bool/Type[])，属同一路由缺陷的浅层变体；(D) 16 项 receiver 与第二参均真实、仍抛 NotSupportedException，集中于 Assembly/Module 流式重载与 Type/TypeInfo 路径，与记忆中的 typeof 折叠 / 对象模型错配架构级阻断一致，**不能靠 ATG 修**；(E) 4 项 AmbiguousMatchException 与 (F) 13 项 ArgumentException 源于「typeof(ReflectionSubjectSample) 被当作 attribute 的 Type 实参」——语义选错种子类型，需 ATG 为 attribute Type 位置提供真实 Attribute 子类。真实实例工厂可行性结论：B+C 共 23 项（21%）可通过 ATG 修（复用现有 19 类型工厂 + 修路由顺序）回收；A 51 项不算缺陷；D+E+F 共 33 项即使给足真实 receiver 仍失败，根因在 native/架构层，不应计入 receiver 供给修复预算。

### [Create<T> 未初始化前置集：Set0 全部参数为 default(T)!（有意负向测试）] n=51 factoryCandidate=False

- fix: 无需修：Set0 是设计上的 ArgumentNullException 负向用例。若接线 AOT 真实语义，应将其改为 Assert.Throws<ArgumentNullException> 而非 stubGap；当前仅因 AOT 无 body 才被记 42
- evidence: testing/foundation-dll/System.Private.CoreLib/chunks/reflection/managed/.autogen/System_Reflection_CustomAttributeExtensions/CustomAttributeExtensions.AutoGenerated.cs:210-215（GetCustomAttribute_8_MemberInfo_System_Type_bool_0 三参全 default）+ ValueGenerator.cs:224-226 Set0 由 paramTypes.Select(DefaultValue) 生成

### [Create<T> 未初始化 / receiver 仍为 default(...)!，但类型已在 ReflectionInstanceFactories 表中有条目 —— 路由/顺序缺陷] n=16 factoryCandidate=True

- fix: 复用表内既有表达式：typeof(ReflectionSubjectSample) / typeof(ReflectionSubjectSample).GetMethod("SampleMethod")!.GetParameters()[0]（与 c61ba23d6 的 19 类型工厂完全一致）；修法是在 ValueGenerator 的 Set1/boundary 集生成路径上也让参数经过 TryGetResolvableInstanceExpression（当前 default/boundary 集绕过 smart 集 → ValueGenerator.cs:225-242 先于 :286 产出 default(T)）
- evidence: generated: CustomAttributeExtensions.AutoGenerated.cs:213,222（GetCustomAttribute_8/9/10/11/16/17/26/27/28/29、IsDefined_34/35 的 Set1）+ RuntimeReflectionExtensions.AutoGenerated.cs（GetRuntimeField/Method/Property/Event 的 Set1）；表条目见 ValueGenerator.cs:936-950（MemberInfo/ParameterInfo/Type 均在表内）

### [静态方法无 receiver，但第二参（attribute 的类型实参 Type / bool）仍为 default] n=7 factoryCandidate=True

- fix: bool 实参给字面量 false（inherited 语义位），Type[] 实参给 new[]{ typeof(ReflectionSubjectSample) }；属同一路由缺陷的浅层变体，与 B 组同批修
- evidence: generated: GetCustomAttribute_8_..._2（typeof(ReflectionSubjectSample), typeof(ReflectionSubjectSample), default(bool)）等 6 项 + GetRuntimeMethod_5_..._3（new System.Type[2]{default(Type)!,default(Type)!}）；第一参已是 typeof(sample) 真实实例

### [真实实例 receiver + 真实第二参，仍抛 NotSupportedException（receiver 供给已正确，根因不在 ATG）] n=16 factoryCandidate=False

- fix: 无 ATG 侧修法。根因是 native 侧 Assembly/Module 流式自定义属性路径与 Type/TypeInfo 对象模型未实现（与记忆 reflection-neg-value-tagged-pointer-abi / typeof-fold-vs-object-model-mismatch 同一架构阻断），需 native 实现 GetCustomAttributes(Assembly/Module) 与 RuntimeReflectionExtensions 的 TypeInfo 分派
- evidence: GetCustomAttributes_12/13/14/22/23/24（Assembly.GetExecutingAssembly() / .ManifestModule / typeof(sample)）+ CustomAttributeData.GetCustomAttributes_0/1/2 + RuntimeReflectionExtensions.GetRuntimeFields/Methods/Properties/Events_1 + GetRuntimeBaseDefinition_8_1，全部 receiver 为真实实例

### [真实实例 + AmbiguousMatchException（选择器歧义：无参重载与泛型重载同时命中）] n=4 factoryCandidate=True

- fix: receiver 已真实；失败原因是同时存在 GetCustomAttribute(Assembly) 与 GetCustomAttribute<T>(Assembly) 两个候选而 CLR 报歧义（真实 .NET 亦如此）。应让 ATG 为无 Type 重载显式指定 <System.Attribute> 泛型实参或改断言为 Throws<AmbiguousMatchException>
- evidence: GetCustomAttribute_4/5/6_1（Assembly/Module/typeof(sample) 的无 Type 重载）+ GetCustomAttribute_10_MemberInfo_bool_2（泛型 <Attribute> + bool）

### [参数全真实，仍 ArgumentException —— 语义选错种子类型（typeof(ReflectionSubjectSample) 被当作 attribute 的 Type 实参）] n=13 factoryCandidate=True

- fix: 为「attribute 类型」这一语义位置提供真实 Attribute 子类的 typeof：ProbeEmitter 的 ReflectionSubjectSample 需加 [Serializable] 等特性并新增 public sealed class ReflectionSubjectAttribute : System.Attribute，然后用 typeof(ReflectionSubjectAttribute)；同时 receiver 保持 typeof(ReflectionSubjectSample)
- evidence: GetCustomAttribute_0/1/2/3_1、GetCustomAttributes_18/19/20/21_1、IsDefined_30/31/32/33_1、GetRuntimeInterfaceMap_9_1，形如 GetCustomAttribute(Assembly.GetExecutingAssembly(), typeof(ReflectionSubjectSample))

## R2-type-assembly

R2 (Type/TypeInfo/Assembly/Module) covers 188 of the 390 stubGap subjects. Contrary to the S1 premise, receiver supply is ALREADY FIXED here: 152/188 probes use a real metadata-backed receiver (typeof(int).Assembly.X(), typeof(int).Module.X(), new TypeDelegator(typeof(ReflectionSubjectSample)).X(), Assembly.GetExecutingAssembly().GetName().X()); only 2 still use SubjectInstanceFactory.Create<T>() and 34 have no receiver at all (genuinely static entry points). The stubGap label is NOT receiver-driven — it is ATG's exception rule: AOT-STUB-GAP count == probe hasException count exactly, in all 8 family dirs (e.g. Module 51/51, Assembly 84/84, TypeDelegator 32/32, TypeInfo 6/6). Proof: typeof(int).Module.GetTypes() has a real receiver and NO null args, yet still records System.NotSupportedException. Module.Resolve* with valid int tokens record ArgumentException/ArgumentOutOfRangeException. These are legitimate BCL input-driven exceptions, so ATG cannot assert against a native stub returning default and emits `return 42L;`. Fixing receivers in this family yields ZERO additional real facts. The 2 Create<T> cases (AssemblyNameProxy) and the 34 static cases are NOT receiver defects either: AssemblyNameProxy is a real instantiable type whose Create<T> succeeds, and the 34 static ones (Assembly.Load/GetCallingAssembly/GetEntryAssembly/GetAssembly/GetExecutingAssembly, AssemblyName.GetAssemblyName, IntrospectionExtensions.GetTypeInfo) fail because Load/GetAssemblyName are unsupported-by-design on a single-file AOT runtime, and GetExecutingAssembly/GetTypeInfo(null) throw NotSupportedException/ArgumentNullException — also input/design driven, not receiver driven. Recommendation: do NOT apply the c61ba23d6 real-instance-factory fix to this family; it is already applied. Route R2 stubGaps to an ATG exception-assertion capability (assert "throws X") instead, or classify them unassertable.

### [Create<T> 未初始化 (GetUninitializedObject) — 未修复] n=2 factoryCandidate=True

- fix: 仅 AssemblyNameProxyTests.GetAssemblyName_0_string_0/_1 (System.Reflection.AssemblyNameProxy)。该类有公共无参 ctor 且非抽象，Create<T> 实际走 Activator.CreateInstance<T>() 而非 GetUninitializedObject，因此不是元数据缺失型假体。真实实例表达式: new global::System.Reflection.AssemblyNameProxy()（或保持 Create<T>，等价）。注意例外为 ArgumentNullException/ArgumentException 由 null/空路径参数驱动，与 receiver 无关。
- evidence: D:\agent\chaos-il2cpp\testing\foundation-dll\System.Private.CoreLib\chunks\reflection\managed\.autogen\System_Reflection_AssemblyNameProxy\.probe\Probe.cs:61 与 :73 均为 SubjectInstanceFactory.Create<System.Reflection.AssemblyNameProxy>().GetAssemblyName(...)

### [真实实例 (typeof(...).Assembly / typeof(...).Module / GetExecutingAssembly().GetName() / new TypeDelegator(typeof(...)) / ty] n=152 factoryCandidate=False

- fix: N/A — receiver 已为真实元数据实例。真正阻断在 ATG 的异常规则: 探针记录到异常时发射 `return 42L;`(AOT-STUB-GAP)，native 从未被驱动。修法不在 receiver，而在 native 侧实现这些元数据方法并让 ATG 支持异常断言。
- evidence: ModuleTests 51/51 全部 real-instance, 例: D:\agent\chaos-il2cpp\testing\foundation-dll\System.Private.CoreLib\chunks\reflection\managed\.autogen\System_Reflection_Module\.probe\Probe.cs:61 typeof(int).Module.IsResource() / :74 typeof(int).Module.IsDefined(...); TypeDelegatorTests 32/32 例: System_Reflection_TypeDelegator\.probe\Probe.cs:61 new global::System.Reflection.TypeDelegator(typeof(ReflectionSubjectSample)).IsAssignableFrom(...); TypeInfoTests 6/6 例: System_Reflection_TypeInfo\.probe\Probe.cs 中 typeof(ReflectionSubjectSample).GetTypeInfo().GetDeclaredX(...); MemberInfoTests 8/8 例 typeof(object).GetMembers(...)[0].HasSameMetadataDefinitionAs(...); AssemblyTests 54/84 例 typeof(int).Assembl

### [静态无 receiver (真正的静态入口, receiver 供给不适用)] n=34 factoryCandidate=False

- fix: N/A — 这些是静态方法, 没有 receiver 可供给。Assembly.GetExecutingAssembly()/GetCallingAssembly()/GetEntryAssembly()/GetAssembly(type)/GetTypeInfo(null) 在单文件 AOT 运行时按设计拋 NotSupportedException/ArgumentNullException; Assembly.Load/LoadFile/LoadFrom/LoadWithPartialName/ReflectionOnlyLoad/ReflectionOnlyLoadFrom/UnsafeLoadFrom 按设计不可用。属 ATG unassertable 或 native 不实现类, 非 receiver 缺陷。
- evidence: 34 项: AssemblyTests 30 项 (Probe.cs 中 global::System.Reflection.Assembly.Load(default(string)!)/LoadWithPartialName/GetExecutingAssembly/GetCallingAssembly/GetAssembly/GetEntryAssembly/LoadFile/LoadFrom/UnsafeLoadFrom/ReflectionOnlyLoad/ReflectionOnlyLoadFrom) + System_Reflection_AssemblyName\.probe\Probe.cs (AssemblyName.GetAssemblyName(default(string)!)) 2 项 + System_Reflection_IntrospectionExtensions\.probe\Probe.cs (IntrospectionExtensions.GetTypeInfo(default(System.Type)!)) 2 项

### [其他 (receiver 存在但被 methodIndex/setIndex 混淆的次级形态)] n=0 factoryCandidate=False

- fix: 无
- evidence: 188 项 100% 映射到三类之一, unmatched=0 (基于 Probe.cs 全量 var result 语句与 AutoGenerated.cs 的 [Fact] 方法名精确 join)

## R4-rest

R4-rest = 390 stubGap 中除去 TypeDelegator(已真实实例) 之外的其余全部，共 358 项，分属 18 个 subject 类（Assembly 84 / CustomAttributeExtensions 78 / Module 51 / PropertyInfo 25 / Binder 23 / RuntimeReflectionExtensions 22 / EventInfo 14 / MethodInfo 11 / MemberInfo 8 / CustomAttributeData 7 / FieldInfo 7 / ParameterInfo 6 / TypeInfo 6 / ReflectionContext 5 / MethodBase 4 / AssemblyName 3 / AssemblyNameProxy 2 / IntrospectionExtensions 2）。

关键发现（部分推翻任务前提）：R4-rest 里纯粹的 receiver 供给问题基本不存在。把 390 条 stubGap 逐条回溯到 managed/combined/CombinedSubjects.cs 的 AOT-STUB-GAP 注释行、按 receiver 根表达式分类后：D 类「receiver 已是真实实例表达式」213 项（含 TypeDelegator 32）；B 类「扩展方法静态服务，receiver 是首个参数」102 项；C 类「纯静态无 receiver」45 项；A 类「Create<T> 未初始化实例」仅 30 项（Binder 23 + ReflectionContext 5 + AssemblyNameProxy 2）。即 c61ba23d6 的 KnownTypeFactories 已把 19 个反射成员类型 + TypeDelegator 全部接上真实实例表达式，R4-rest 中真正残留 Create<T> 未初始化主体的只有 30 项，而这 30 项（Binder/ReflectionContext 是抽象基类、AssemblyNameProxy 自身在 AOT 下即抛 NotSupportedException）在 BCL 中没有可复制的真实实例表达式，真实实例工厂对 R4-rest 的边际收益约等于 0。

真正阻塞 R4-rest 358 项的是第二层机制，且项目已掌握其解法：TestEmitter.ExternalStubRaisesManagedException() 是一条逐 (类型,方法) 白名单。命中者发射 try { call; throw new Exception("AOT stub did not throw"); } catch { }（TestEmitter.cs:468-470，即 json-xml 线的 F 类转真路径，历史收益 29→58、256→264）；未命中者直接落 // AOT-STUB-GAP + return 42L（TestEmitter.cs:483-484）。该白名单当前只覆盖 JsonMetadataServices / XmlTextWriter / XmlWriter / XmlConvert / XmlTextReader / XmlDocument 等 XML/JSON 类型，reflection 命名空间下一条都没有。所以 R4-rest 本质是「注释驱动的实现清单」问题（可称 E 类），不是 receiver 问题。

另有一条对 213 项 D 类成立的次要虚假模式：AOT-STUB-GAP 注释所描述的异常，其 receiver 是被拦截标的方法的返回值（Assembly.GetExecutingAssembly() → Assembly、Assembly.GetName() → AssemblyName、MethodInfo.CreateDelegate()/MakeGenericMethod() 的返回值），该方法仍走外部 catch-all 返回 default/null，随后在 receiver 上执行真实 BCL 代码时抛 NRE；探针记录的是链尾异常，注释却把链首调用写成抛出者。这会让「用真实实例重探」得到看似不同、实为同一非实现缺陷的证据（对应记忆 refection-neg-value-tagged-pointer-abi / b7-reflection-chunk-final-triage 的同类模式），排查时必须按链尾归因。

分层建议：(1) 先对 B/C/D 三类做一次 receiver 无关的验证，确认 ATG 侧 receiver 已不再是瓶颈；(2) 真正的主修点在 TestEmitter 白名单扩容（reflection 族整族加入）+ native/Codegen 侧为这些方法补 shape/body，属 CodeGen+ATG+native 三域，应按 multi-domain workflow 走；(3) 不建议再为 R4-rest 新增真实实例工厂条目。

### [D_expr_propagated — receiver 已是真实实例表达式（typeof(T).X 链 / new TypeDelegator(typeof(ReflectionSubjectSample)) / ReflectionSu] n=213 factoryCandidate=False

- fix: 无需替换 receiver。真实缺口在 TestEmitter.cs:483 的 AOT-STUB-GAP 分支 —— 应比照 468-470 行的 try{call;throw}catch{} 形态转为可断言，或按 (类型,方法) 补 native shape；另需按链尾重新归因注释（例 Assembly.GetExecutingAssembly() 返回 null → .GetName() 抛 NRE，注释却写成 GetName 抛 JsonException）
- evidence: artifacts/foundation-dll/System.Private.CoreLib/chunks/reflection/managed/combined/CombinedSubjects.cs:390 条 AOT-STUB-GAP 中的 213 条；例 typeof(int).Assembly.GetTypes()、typeof(string).GetProperty("Length")!.SetValue(default(object)!,default(object)!)、new global::System.Reflection.TypeDelegator(typeof(ReflectionSubjectSample)).InvokeMember(...)；生成器表见 src/tools/Chaos.IL2CPP.Tools.AutoTestGenerator/CSharpExpressionBuilder.cs:472-517

### [B_extension_static_noreceiver — 扩展方法/静态服务类，receiver 是被调 API 的首个参数（default(T)! 或 typeof(...) 表达式），无实例概念] n=102 factoryCandidate=False

- fix: 无 receiver 可换。39 项首参已是 default(T)! → 首值集本就该抛 ANE，属 null_arg 族（fact_chunk.py _get_null_arg_subject_ids），应走 TestEmitter.cs:534-548 的 hasNullDefaultRefArg → catch-any Throws 分支；其余 63 项首参为真实表达式，需补 shape/body
- evidence: CustomAttributeExtensions 78 + RuntimeReflectionExtensions 22 + IntrospectionExtensions 2；例 global::System.Reflection.CustomAttributeExtensions.GetCustomAttribute(default(System.Reflection.Assembly)!, default(System.Type)!)、Assembly.GetExecutingAssembly().ManifestModule 作首个实参的 GetCustomAttribute<System.Attribute>(...) 抛 AmbiguousMatchException

### [C_static_noreceiver — 纯静态方法（Assembly.Load/LoadFrom/LoadFile/ReflectionOnlyLoad/GetExecutingAssembly/GetCallingAssembly/G] n=45 factoryCandidate=False

- fix: 无 receiver 可换。Assembly.Load(string/byte[]) 有明确定义语义（null→ANE、空/坏格式→ArgumentException/BadImageFormatException），建议加入 ExternalStubRaisesManagedException 白名单并补对应 native stub；Assembly.GetCallingAssembly/GetExecutingAssembly 应复用已有 DiscriminatingExpectations 判别式断言
- evidence: global::System.Reflection.Assembly.Load(default(string)!)、global::System.Reflection.Assembly.GetCallingAssembly()、global::System.Reflection.FieldInfo.GetFieldFromHandle(default(System.RuntimeFieldHandle)!) 等；其中 Assembly.GetCallingAssembly/GetExecutingAssembly 已在 TestEmitter.cs:30-32 有 DiscriminatingExpectations 条目，却仍落 AOT-STUB-GAP

### [A_Create<T>_uninit — SubjectInstanceFactory.Create<T>() 真正未初始化主体，是 R4-rest 内唯一符合任务描述的 receiver 供给缺陷] n=30 factoryCandidate=False

- fix: 无可行的真实实例表达式：Binder 与 ReflectionContext 是 abstract 基类（GetUninitializedObject 出抽象实例），AssemblyNameProxy 的 managed 实现在 AOT/单文件场景本身就抛 NotSupportedException(GetAssemblyName) —— 后者已在生成物中体现为真实异常而非缺口。建议这 30 项按「抽象类型 → 合成子类」既有机制处理（CSharpExpressionBuilder.cs:557-575 TryGetAbstractSubclassExpression），或直接划为 unassertable/架构边界，不值得新增 KnownTypeFactories 条目
- evidence: SubjectInstanceFactory.Create<System.Reflection.Binder>() 23 项（BinderTests::BindToField/BindToMethod/ChangeType/SelectMethod/SelectProperty/ReorderArgumentArray）、Create<System.Reflection.ReflectionContext>() 5 项、Create<System.Reflection.AssemblyNameProxy>() 2 项；生成物见 System_Reflection_Binder/Binder.AutoGenerated.cs（23 处 Create<）、System_Reflection_ReflectionContext/、System_Reflection_AssemblyNameProxy/

### [E_class_whitelist_gap — 跨全部 358 项的真正结构性根因：AOT-STUB-GAP 是 ATG 的『无 AOT body』判定，由逐 (类型,方法) 白名单驱动，与 receiver 无关] n=358 factoryCandidate=False

- fix: 分两步：(1) 复制 json-xml F 类先例，在 ExternalStubRaisesManagedException 中新增 System.Reflection.* 分支 → 发射 try{call;throw new Exception("AOT stub did not throw");}catch{}，把「无 body」变成「断言两边都抛」；(2) 对返回值型方法按 Codegen/native 补 shape（manifest 覆盖后才可判 realDefect）。跨 ATG+Codegen+native 三域，应按多域 workflow 委托
- evidence: src/tools/Chaos.IL2CPP.Tools.AutoTestGenerator/TestEmitter.cs:190 isExternalAssembly（非 Chaos.TestFramework.Sdk 的全部真）、:478 未命中 ExternalStubRaisesManagedException 即 emit AOT-STUB-GAP、:807-1020 ExternalStubRaisesManagedException 白名单全文（无任何 System.Reflection.* 条目）；接收侧判定见 tests/e2e/verification/stages/fact_chunk.py:470 _stub_gap_method_ids 与 :368 stubGap 分类

## R1-memberinfo

R1-memberinfo 族（MemberInfo/MethodBase/MethodInfo/ConstructorInfo/PropertyInfo/FieldInfo/EventInfo/ParameterInfo）在 reflection chunk 中占 75 个 stubGap subject（占全 chunk 390 的 19.2%）。核心判定：**receiver 供给不是本族的根因，真实实例工厂已经落地且已生效**。按 receiver 表达式分类：70/75（93.3%）已经使用 c61ba23d6 引入的真实实例工厂（typeof(...)/GetExecutingAssembly()/GetMethod(...)/GetProperty(...)/GetEvents()[0] 等），5/75（6.7%）是静态方法天然无 receiver（GetFieldFromHandle ×2、GetMethodFromHandle ×2、GetCurrentMethod），0 个仍是 Create<T> 未初始化形态。真正的 stubGap 根因是 ATG TestEmitter.cs:478 的 isExternalAssembly 兜底分支——当 managed 端探针记录到这些方法抛异常、而 ExternalStubRaisesManagedException 白名单（TestEmitter.cs:807，仅覆盖 JsonMetadataServices 的 11 个 Create*Info）未命中时，直接发射 `return 42L` 并打上 "// AOT-STUB-GAP" 标记，整个方法体被替换为常量。证据链闭合：artifacts/.../reflection/build_jit_output/codegen/generated/native-aot.page-0005.cpp 中 GetAccessors_1__0 的生成体只有三行有效语句（_s0 = 42 → ChaosStoreInt64 → return _i0），没有任何 ChaosReflection 调用；而 native 侧的实现其实早已存在（src/native/runtime-core/reflection/remaining_stubs.cpp:1090 ChaosReflectionPropertyGetAccessors、:1510 ChaosReflectionEventGetOtherMethods、:800 GetGetMethod、:371/379/387 GetModified*Type），ApiSurfaceScanner 的映射表也齐备（Classifier.cs:324 PropertyInfo.GetAccessors→ChaosReflectionPropertyGetAccessors、:219 GetModifiedPropertyType），但 RuntimeHelperShapeRegistry 里**这些方法全部没有 registry.Register 条目**（grep 证实：PropertyInfo 仅注册了 get_CanRead/get_CanWrite/get_PropertyType/GetIndexParameters/GetRawConstantValue/GetSetMethod 六个，MethodInfo 仅 GetBaseDefinition/MakeGenericMethod 两个）。因此 S1 对本族的正确修复方向不是新增 receiver 工厂（那部分已完成于 c61ba23d6），而是 (a) 在 RuntimeHelperShapeRegistry 补齐这 75 个方法的 shape 注册使其接到既有 native 符号，(b) 收紧 TestEmitter 的 stubGap 兜底使其只在真正缺失实现时生效。建议 S1 把本族划归为 shape 注册类修复，而非 receiver 供给类。

### [真实实例工厂（typeof(T)/GetExecutingAssembly()/GetMethod()/GetProperty()/GetEvents()[0]）—— 接收者已是带真实元数据的实例，工厂工作已完成，无需再改 receiver] n=70 factoryCandidate=True

- fix: 无需替换 receiver 表达式。改为在 RuntimeHelperShapeRegistry 注册 shape 使既有 native 符号被接线，或把该成员加入 TestEmitter.ExternalStubRaisesManagedException 白名单使其走 try{call;throw} 而非 return 42L。已注册的同族对照（走通、resultKind=real）：PropertyInfo.GetIndexParameters/GetRawConstantValue/GetSetMethod、PropertyInfo.GetOptionalCustomModifiers、MethodBase.GetParameters/GetMethodBody/GetMethodImplementationFlags、FieldInfo.GetRawConstantValue/GetCustomAttributes —— 说明差别确实在 shape 注册与白名单，不在 receiver
- evidence: D:\agent\chaos-il2cpp\testing\foundation-dll\System.Private.CoreLib\chunks\reflection\managed\.autogen\System_Reflection_PropertyInfo\PropertyInfo.AutoGenerated.cs:79 (typeof(string).GetProperty("Length")!.GetAccessors()); System_Reflection_EventInfo\EventInfo.AutoGenerated.cs:62 (typeof(System.ComponentModel.PropertyChangedEventArgs).GetEvents()[0].GetOtherMethods()); System_Reflection_MemberInfo\MemberInfo.AutoGenerated.cs:63/87/120 (typeof(object).GetMembers(BindingFlags.Public|BindingFlags.Instance)[0].*); System_Reflection_MethodInfo\MethodInfo.AutoGenerated.cs: 中 typeof(string).GetMethod("ToString", Type.EmptyTypes)!.CreateDelegate(...); System_Reflection_ParameterInfo (typeof(string).

### [静态方法无 receiver（FieldInfo.GetFieldFromHandle / MethodBase.GetMethodFromHandle / MethodBase.GetCurrentMethod）—— 天然无实例接收者，r] n=5 factoryCandidate=False

- fix: 无法用 receiver 工厂修复。手写 C# 测试须提供真实 RuntimeFieldHandle/RuntimeMethodHandle（如 typeof(Sample).GetField("SampleField")!.FieldHandle / typeof(Sample).GetMethod("SampleMethod")!.MethodHandle，即 F2-fixture 方案），或在 native 层把 chaos_*Handle 直接解析成对应描述符。GetCurrentMethod 须在 native 侧返回当前栈帧方法描述符
- evidence: D:\agent\chaos-il2cpp\testing\foundation-dll\System.Private.CoreLib\chunks\reflection\managed\.autogen\System_Reflection_FieldInfo\FieldInfo.AutoGenerated.cs:60-74 (global::System.Reflection.FieldInfo.GetFieldFromHandle(default(System.RuntimeFieldHandle)!))；System_Reflection_MethodBase\MethodBase.AutoGenerated.cs (GetMethodFromHandle ×2 / GetCurrentMethod)

### [Create<T> 未初始化（SubjectInstanceFactory.Create<T> = GetUninitializedObject）—— 本族**计数为 0**；c61ba23d6 已把全部反射成员类型的工厂替换为真实实例，残] n=0 factoryCandidate=True

- fix: 本族无需新增。若要做残余 31 个 Create<T>，照抄 c61ba23d6 的 KnownTypeFactories CustomExpr 模式；种子类型 ReflectionSubjectSample 由 ProbeEmitter.cs:387-405 发射（含 SampleField/SampleConst/SampleProperty/SampleEvent/SampleMethod + IReflectionSubject），注意 ProbeEmitter 与 TestEmitter 必须同时发射否则 CS0246 → 空 probe-results.json → 整类静默降级（该缺陷已在 ProbeEmitter.cs:380-386 注释中记录）
- evidence: 对 .autogen/System_Reflection_{MemberInfo,MethodBase,MethodInfo,ConstructorInfo,PropertyInfo,FieldInfo,EventInfo,ParameterInfo}/*.AutoGenerated.cs 全量扫描 75 个 AOT-STUB-GAP 注释行，正则 SubjectInstanceFactory.Create< 命中数为 0（对照：全 reflection 域 390 个 stubGap 中 31 个是 Create<T>）。先例实现见 src/tools/Chaos.IL2CPP.Tools.AutoTestGenerator/CSharpExpressionBuilder.cs:462-519 KnownTypeFactories 的 Reflection subjects 段（c61ba23d6）

### [其他 / 根因实为 ATG stubGap 兜底而非 receiver —— 全部 75 项的共同根因通道：managed 探针记录异常 → ExternalStubRaisesManagedException 白名单未命中 → TestE] n=75 factoryCandidate=True

- fix: 两条并行修法：(1) 在 src/managed/Chaos.IL2CPP.Generator/RuntimeSupport/RuntimeHelperShapeRegistry.CoreStubs.*.cs 为 75 个方法补 registry.Register("System.Reflection.<Type>", "<Method>", <paramTypes>, CreateNativeIntAbiSlot(...)) 条目，接到既有 native 符号（照抄 RuntimeHelperShapeRegistry.CoreStubs.Part2.S4.cs:140 的 GetSetMethod 条目范式；取参模式照抄 GetSetMethod 的 ["System.Boolean"]）。须同时修正 native 返回 ABI：PropertyInfo.GetAccessors/GetValue 返回 MethodInfo[]/object[] 托管数组，而 remaining_stubs.cpp:1090 现只返回 getter 句柄（缺数组装箱）——用 T7 验证过的 object_stubs.cpp GetUninitializedObject 双形态解析 + RuntimeEntry 模板补数组构造。(2) 收窄 TestEmitter.cs:478 的 isExternalAssembly 兜底：仅当 ApiSurfaceScanner.Classifier 判定该成员无 native 映射时才打 stubGap，否则发射真实 Assert。二者缺一不可——只补 shape 而兜底仍在，ATG 侧生成物依然是 return 42L（生成物来自 ATG 编译的 managed 探针程序集，与 native shape 无关），因此 (2) 是让修复可见于 fact 的必要条件。
- evidence: D:\agent\chaos-il2cpp\src\tools\Chaos.IL2CPP.Tools.AutoTestGenerator\TestEmitter.cs:478-489（isExternalAssembly 兜底发射 AOT-STUB-GAP + return 42L）、:190（isExternalAssembly = !assemblyName.StartsWith("Chaos.TestFramework.Sdk")）、:807-818（ExternalStubRaisesManagedException 白名单仅覆盖 JsonMetadataServices 的 11 个 Create*Info）；生成物铁证 D:\agent\chaos-il2cpp\artifacts\foundation-dll\System.Private.CoreLib\chunks\reflection\build_jit_output\codegen\generated\native-aot.page-0005.cpp 的 CombinedSubjects_..._PropertyInfoTests_GetAccessors_1__0 函数体仅含 `_s0 = static_cast<CHAOS_IL2CPP_INTPTR>(42); _i0 = ChaosStoreInt64(...); return _i0;`，无任何 ChaosReflectionPropertyGetAccessors 调用；native 实现却已存在：src/native/runtime-core/


**总计（含族间重叠）**: 1193
