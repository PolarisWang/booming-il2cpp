# scripts-orphaned-2026-08-27

Orphaned one-shot scripts archived from `scripts/codegen/` during the repo-cleanliness
optimization. They were unreferenced by any code/CI/workflow/CMake/docs (verified via
git grep) — historical ad-hoc utilities. Git history preserves full content if any
future need arises. Do NOT restore into scripts/ unless actually invoked.
