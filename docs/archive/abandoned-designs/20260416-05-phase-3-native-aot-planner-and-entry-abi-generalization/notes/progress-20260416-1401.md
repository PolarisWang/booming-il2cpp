# 2026-04-16 14:01 Phase 3 进展记录

## 本轮完成

- 完成 `tests/unit/compatibility/test_phase3_async_task_builder_native_aot.py` 的 planner / emitter 收口：
  - catch-only EH shape 允许 async `MoveNext()` try 体内部多处 `leave`
  - structured EH prefix linear lowering 增补 `ldarg` / `ldfld`
  - planner 增补 `brfalse`
  - `callvirt` 的 `HybridDispatchKind.ExternalRuntime` 并入 direct helper bridge
- 落地最小 `ldstr` native-aot runtime：
  - planner 增补 `ldstr` opcode lowering
  - object model 增补 `System.String` tracking
  - native string object 形状增加 `length` / `utf8_data`
  - JSON 反序列化后的 string operand 已补齐读取
- 聚合 `CoreRuntimeFeatures/ProofEntry::Run()` 的首阻塞继续前推到 `ldtoken`

## 验证

- `python -m pytest tests/unit/compatibility/test_phase3_async_task_builder_native_aot.py -q`
  - `2 passed`
- `python -m pytest tests/unit/compatibility/test_phase4_generic_constraint_dispatch_native_aot.py tests/unit/compatibility/test_phase4_generic_dispatch_native_aot.py tests/unit/compatibility/test_phase4d_dispatch_aot_core_ir.py tests/unit/compatibility/test_phase4d_dispatch_native_aot.py tests/unit/compatibility/test_phase3_direct_callvirt_native_aot.py tests/unit/compatibility/test_phase3_async_task_builder_native_aot.py tests/unit/compatibility/test_mainline_capability_batch_a.py tests/unit/compatibility/test_mainline_capability_batch_b.py tests/unit/compatibility/test_phase4_aot_core_ir_contract.py tests/unit/compatibility/test_phase2_aot_core_ir_ldtoken_target_reference.py -q`
  - `25 passed`
- `dotnet build src/managed/Chaos.IL2CPP.Driver/Chaos.IL2CPP.Driver.csproj -c Release -m:1`
  - 成功
- `dotnet <Driver.dll> <CoreRuntimeFeatures.dll> <bundle> --entry-point-subject-id CoreRuntimeFeatures/ProofEntry::Run() --additional-assembly src/reference/Chaos.TestFramework/bin/Release/net8.0/Chaos.TestFramework.dll`
  - 成功
- `dotnet <Driver.dll> emit-native-aot <bundle> <emit>`
  - 失败，首阻塞变为 `ldtoken`

## 观察

- async external-runtime family 已不再是主线首阻塞，说明这轮 planner / emitter bridge 的方向正确。
- `ldstr` 只补了最小 literal object model；后续 string-heavy owner 很可能继续暴露 `String.Concat` / `String.Join` / `String::op_Equality` 等 helper family。
- 当前主线已进入 metadata token materialization 前沿，`ldtoken` 更像一个新的 capability family，而不是单一 opcode 补洞。

## 下一步

- 先冻结 `AotClosureManifest::BuildRequiredClosure()` 的 `ldtoken` native-aot failing surface，再决定是否要把 metadata token materialization 拆成新的 child task。
