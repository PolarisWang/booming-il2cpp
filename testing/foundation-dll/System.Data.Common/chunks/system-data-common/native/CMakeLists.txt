# Generated by Chaos.IL2CPP.Tools.TestProjectGenerator
# DO NOT EDIT — regenerated on each generate

cmake_minimum_required(VERSION 3.20)
project(chaos_entry CXX)
set(CMAKE_CXX_STANDARD 20)

# ── Compiler settings ──
# /EHa needed for catch(...) to intercept C++ exceptions thrown by generated code.
if(MSVC)
  add_compile_options(/utf-8 /GS- /FS /bigobj /wd4716 /MP)
  set(CHAOS_EHA_FLAG "/EHa")
  set(CHAOS_FORCE_MULTIPLE /FORCE:MULTIPLE)
else()
  add_compile_options(-Wall -Wextra -Wno-unused-parameter -fpermissive)
  set(CHAOS_EHA_FLAG -fexceptions)
  # eager_compile.cpp is compiled locally AND present in prebuilt
  # libchaos_interpreter.a — both define EagerCompilePatchMethods.
  set(CHAOS_FORCE_MULTIPLE "-Wl,--allow-multiple-definition")
endif()

# ── Config tier ──
if(NOT DEFINED CHAOS_IL2CPP_CONFIG_TIER)
  set(CHAOS_IL2CPP_CONFIG_TIER "check")
endif()

# ── Hephaestus Lib: pre-compiled framework cache ──
# When CHAOS_HEPHAESTUS_DIR is set, link against pre-compiled static libraries
# from previously-cached builds.  This avoids recompiling the IL2CPP codegen
# output for core assemblies (System.Private.CoreLib, System.Runtime, etc.)
# across chunks.
# The cache is managed by the HephaestusCache in the Python verification pipeline.
# If the variable is not set, the build proceeds as normal.
if(DEFINED CHAOS_HEPHAESTUS_DIR AND EXISTS "${CHAOS_HEPHAESTUS_DIR}")
  message(STATUS "Hephaestus Lib: ${CHAOS_HEPHAESTUS_DIR}")
  file(GLOB CHAOS_HEPHAESTUS_LIBS "${CHAOS_HEPHAESTUS_DIR}/*.lib" "${CHAOS_HEPHAESTUS_DIR}/*.a")
  if(CHAOS_HEPHAESTUS_LIBS)
    list(APPEND CHAOS_ENTRY_SOURCES ${CHAOS_HEPHAESTUS_LIBS})
    message(STATUS "Hephaestus Lib: ${CHAOS_HEPHAESTUS_LIBS}")
  endif()
endif()
if(CHAOS_IL2CPP_CONFIG_TIER STREQUAL "profile")
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_TIER=CHAOS_IL2CPP_CONFIG_TIER_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_LOG_LEVEL=3)
  add_compile_definitions(CHAOS_IL2CPP_RUNTIME_CONFIG_FILE="runtime-config/desktop.h")
  add_compile_definitions(CHAOS_IL2CPP_PROFILE_ENABLED=1)
elseif(CHAOS_IL2CPP_CONFIG_TIER STREQUAL "ship")
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_TIER=CHAOS_IL2CPP_CONFIG_TIER_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_LOG_LEVEL=3)
  add_compile_definitions(CHAOS_IL2CPP_RUNTIME_CONFIG_FILE="runtime-config/desktop.h")
else()
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_TIER=CHAOS_IL2CPP_CONFIG_TIER_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_CONFIG_CHECK)
  add_compile_definitions(CHAOS_IL2CPP_LOG_LEVEL=3)
  add_compile_definitions(CHAOS_IL2CPP_RUNTIME_CONFIG_FILE="runtime-config/test-only.h")
endif()



# Tier manager: real implementation from tier_manager.cpp is compiled into
# the runtime library, so suppress the stub in registration_globals.cpp.
add_compile_definitions(CHAOS_IL2CPP_HAS_TIER_MANAGER=1)

# ── Verification toggle ──
if(NOT DEFINED CHAOS_IL2CPP_VERIFICATION_ENABLED)
  set(CHAOS_IL2CPP_VERIFICATION_ENABLED "1")
endif()
add_compile_definitions(CHAOS_IL2CPP_VERIFICATION_ENABLED=${CHAOS_IL2CPP_VERIFICATION_ENABLED})

# ── Paths ──
set(CHAOS_SDK_DIR "D:/agent/chaos-il2cpp/testing/foundation-dll/System.Data.Common/chunks/system-data-common/native/codegen")
set(CHAOS_PROJECT_ROOT "D:/agent/chaos-il2cpp")
set(CHAOS_CODEGEN_DIR "D:/agent/chaos-il2cpp/testing/foundation-dll/System.Data.Common/chunks/system-data-common/native/codegen")

# Find chaos SDK — provides chaos::runtime (prebuilt libs + flags)
find_package(chaos REQUIRED PATHS "${CHAOS_SDK_DIR}")

# ── Source files ──
# Prefer flat subjects/*.cpp (copied locally by TPG for MAX_PATH compatibility).
# Fall back to codegen_dir/*Subjects/generated/*.cpp for in-place codegen builds.
file(GLOB CHAOS_CODEGEN_FLAT_CPP "${CMAKE_CURRENT_SOURCE_DIR}/subjects/*.cpp")
file(GLOB CHAOS_CODEGEN_NATIVE_CPP
    "${CHAOS_CODEGEN_DIR}/*Subjects/generated/native-aot.generated.cpp"
    "${CHAOS_CODEGEN_DIR}/*Subjects/generated/native-aot.generated.page*.cpp"
    "${CHAOS_CODEGEN_DIR}/*Subjects/generated/native-aot.page-*.cpp"
    "${CHAOS_CODEGEN_DIR}/*Subjects/generated/chaos_generated_module.cpp"
    # Flat layout fallback: codegen writes directly to codegen/generated/
    "${CHAOS_CODEGEN_DIR}/generated/native-aot.generated.cpp"
    "${CHAOS_CODEGEN_DIR}/generated/native-aot.generated.page*.cpp"
    "${CHAOS_CODEGEN_DIR}/generated/native-aot.page-*.cpp"
    "${CHAOS_CODEGEN_DIR}/generated/chaos_generated_module.cpp"
)

if(CHAOS_CODEGEN_FLAT_CPP)
  set(CHAOS_AOT_GENERATED_CPP ${CHAOS_CODEGEN_FLAT_CPP})
else()
  set(CHAOS_AOT_GENERATED_CPP ${CHAOS_CODEGEN_NATIVE_CPP})
endif()

# Native stubs in this directory (exclude TPG-generated and entry files)
file(GLOB CHAOS_NATIVE_STUBS "*.cpp")
list(REMOVE_ITEM CHAOS_NATIVE_STUBS
    "${CMAKE_CURRENT_SOURCE_DIR}/runtime-entry.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/entry.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/dispatch.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/verification_dispatch.generated.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/runtime-patchdata.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/patch-host-arrays.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/chaos_stub_ChaosFindExternalTypeDescByStableId.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/chaos_stub_patch_loader.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/jit_stubs.cpp"
)

# Runtime stubs are provided by libchaos_runtime_core.a (built from source
# on Linux, prebuilt on Windows).  Do NOT compile them locally — the SDK
# library already contains these symbols and compiling them again causes
# multiple-definition linker errors.
# profile_globals.cpp is project-specific, so compile it from source.
# array_stubs.cpp: the prebuilt lib may be stale, missing recently-added
# DirectNativeSymbol entries (ChaosArrayCopy3, ChaosArraySortWithComparer).
# /FORCE:MULTIPLE handles any future overlap when the lib catches up.
set(CHAOS_PROFILE_GLOBALS
    "${CHAOS_SDK_DIR}/runtime_stubs/profile_globals.cpp"
)
set(CHAOS_ARRAY_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/array_stubs.cpp"
)
set(CHAOS_MATH_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/math_stubs.cpp"
)
set(CHAOS_SIMD_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/simd_stubs.cpp"
)
set(CHAOS_CHAR_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/char_stubs.cpp"
)
set(CHAOS_VECTOR_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/vector_stubs.cpp"
)
set(CHAOS_ASYNC_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/async_stubs.cpp"
)
set(CHAOS_MISC_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/misc_stubs.cpp"
)
set(CHAOS_INTEROP_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/interop_stubs.cpp"
)
set(CHAOS_EXCEPTION_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/exception_stubs.cpp"
    "${CHAOS_SDK_DIR}/runtime_stubs/object_stubs.cpp"
    "${CHAOS_SDK_DIR}/runtime_stubs/string_stubs.cpp"
)
set(CHAOS_ENTRY_STUBS
    "${CHAOS_SDK_DIR}/runtime_stubs/entry_stubs.cpp"
)
set(CHAOS_CRYPTO_STUBS
    "${CHAOS_PROJECT_ROOT}/src/native/runtime-core/runtime_stubs/crypto_stubs.cpp"
)
set(CHAOS_ENTRY_SOURCES
    "runtime-entry.cpp"
    "runtime-patchdata.cpp"
    "verification_dispatch.generated.cpp"
    ${CHAOS_NATIVE_STUBS}
    ${CHAOS_PROFILE_GLOBALS}
    ${CHAOS_ARRAY_STUBS}
    ${CHAOS_MATH_STUBS}
    ${CHAOS_SIMD_STUBS}
    ${CHAOS_CHAR_STUBS}
    ${CHAOS_VECTOR_STUBS}
    ${CHAOS_ASYNC_STUBS}
    ${CHAOS_MISC_STUBS}
    ${CHAOS_INTEROP_STUBS}
    ${CHAOS_EXCEPTION_STUBS}
    ${CHAOS_ENTRY_STUBS}
    ${CHAOS_CRYPTO_STUBS}
    ${CHAOS_STRING_STUBS}
    ${CHAOS_AOT_GENERATED_CPP}
)

# ── Include directories ──
set(CHAOS_ENTRY_INCLUDES
    "${CHAOS_PROJECT_ROOT}/src/native/runtime-core"
    "${CHAOS_PROJECT_ROOT}/src/native/runtime-core/gc"
    "${CHAOS_PROJECT_ROOT}/src/native/runtime-core/runtime_stubs"
    "${CHAOS_PROJECT_ROOT}/src/native/common"
    "${CHAOS_PROJECT_ROOT}/src/native/bootstrap"
    "${CHAOS_PROJECT_ROOT}/src/native"
    "${CHAOS_PROJECT_ROOT}/src/native/pal"
    "${CHAOS_PROJECT_ROOT}/src/native/interpreter"
    "${CHAOS_PROJECT_ROOT}/src/native/interpreter/generated"
    "${CHAOS_PROJECT_ROOT}/src/native/support"
    "${CHAOS_PROJECT_ROOT}/src/native/hot-update"
    "${CHAOS_PROJECT_ROOT}/third_party/unordered_dense/include"
    "${CHAOS_PROJECT_ROOT}/cmake"
    "${CHAOS_CODEGEN_DIR}/codegen/generated"

    "${CMAKE_CURRENT_SOURCE_DIR}"
    "${CHAOS_PROJECT_ROOT}/src/native/jit"
    # codegen/generated — needed for enum_metadata.generated.h and other
    # generated headers that aren't copied to subjects/ by TPG's flat layout.
    "${CHAOS_CODEGEN_DIR}/generated"
)

add_executable(chaos_entry ${CHAOS_ENTRY_SOURCES})
if(MSVC)
  target_compile_options(chaos_entry PRIVATE /GS-)
endif()
target_include_directories(chaos_entry PRIVATE ${CHAOS_ENTRY_INCLUDES})
target_compile_options(chaos_entry PRIVATE ${CHAOS_EHA_FLAG})
target_link_libraries(chaos_entry PRIVATE chaos::runtime
    "$<$<CXX_COMPILER_ID:MSVC>:${CHAOS_SDK_DIR}/lib/chaos_pal.lib>"
    "$<$<NOT:$<CXX_COMPILER_ID:MSVC>>:${CHAOS_SDK_DIR}/lib/libchaos_pal.a>"
)
# MSVC: chaos_hot_update.lib references SetPatchedBySlot from
# chaos_runtime_core.lib. Use targeted /INCLUDE instead of
# /WHOLEARCHIVE (which would force ALL objects including the
# old abi_reflection.obj with nullptr ABI entries, overriding
# the locally-compiled fix).
if(MSVC)
  target_link_libraries(chaos_entry PRIVATE bcrypt)

endif()
# OpenSSL: needed by crypto_stubs.cpp on Linux (SHA/HMAC via libcrypto).
if(NOT MSVC)
  find_package(OpenSSL REQUIRED)
  target_include_directories(chaos_entry PRIVATE ${OPENSSL_INCLUDE_DIR})
  target_link_libraries(chaos_entry PRIVATE OpenSSL::Crypto OpenSSL::SSL)
endif()
target_link_options(chaos_entry PRIVATE ${CHAOS_FORCE_MULTIPLE})