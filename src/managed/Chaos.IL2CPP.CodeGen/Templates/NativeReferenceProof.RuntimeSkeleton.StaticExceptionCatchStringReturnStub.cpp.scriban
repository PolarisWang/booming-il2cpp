struct {{ stub_name }}_ManagedArgs
{
    void** return_value;
};

CHAOS_IL2CPP_INT32 CHAOS_RUNTIME_ABI_CALL {{ stub_name }}(
    const CodegenBridgeV0* bridge,
    const CodeRegistrationV0* code_registration,
    const MetadataRegistrationV0* metadata_registration,
    const CodegenRegistrationOptionsV0* options,
    RuntimeState* runtime,
    ThreadState* thread,
    void* managed_args)
{
    if (bridge == nullptr || code_registration == nullptr || metadata_registration == nullptr || options == nullptr || runtime == nullptr || thread == nullptr || managed_args == nullptr) {
        return CHAOS_BRIDGE_STATUS_INVALID_ARGUMENT;
    }
    const RuntimeAbiV0* abi = chaos_runtime_get_abi_v0();
    if (abi == nullptr) {
        return CHAOS_BRIDGE_STATUS_NOT_READY;
    }
    const auto* request = static_cast<const {{ stub_name }}_ManagedArgs*>(managed_args);
    if (request->return_value == nullptr) {
        return CHAOS_BRIDGE_STATUS_INVALID_ARGUMENT;
    }

    void* message = nullptr;
    try {
        const CHAOS_IL2CPP_INT32 throw_status = {{ throw_stub_name }}(
            bridge,
            code_registration,
            metadata_registration,
            options,
            runtime,
            thread,
            nullptr);
        if (throw_status != CHAOS_BRIDGE_STATUS_OK && throw_status != CHAOS_BRIDGE_STATUS_MANAGED_EXCEPTION) {
            return throw_status;
        }
        message = abi->string_new_utf8(runtime, thread, {{ unreachable_literal }}, {{ unreachable_literal_byte_count }}u);
    } catch (const chaos::il2cpp::runtime_core::ManagedExceptionCarrier&) {
        message = abi->string_new_utf8(runtime, thread, {{ caught_literal }}, {{ caught_literal_byte_count }}u);
    }

    if (message == nullptr) {
        return CHAOS_BRIDGE_STATUS_INTERNAL_ERROR;
    }

    *request->return_value = message;
    return CHAOS_BRIDGE_STATUS_OK;
}
