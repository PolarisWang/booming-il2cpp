# Generated by Chaos.IL2CPP.Tools.TestProjectGenerator
# DO NOT EDIT — regenerated on each generate
# chaos-targets.cmake — chaos-il2cpp SDK target definitions
# Loaded by chaos-config.cmake, not meant to be included directly.

# ── chaos::runtime (INTERFACE IMPORTED) ──────────────────────────────────
# Aggregates all prebuilt native runtime libraries + include dirs + flags.
# Consumer links against this to get the full runtime environment.
add_library(chaos::runtime INTERFACE IMPORTED)

target_include_directories(chaos::runtime INTERFACE
    "${CHAOS_SDK_ROOT}/include"
)

# Compile options required by the runtime headers
target_compile_options(chaos::runtime INTERFACE
    "$<$<CXX_COMPILER_ID:MSVC>:/utf-8>"
    "$<$<CXX_COMPILER_ID:MSVC>:/EHa>"
    "$<$<CXX_COMPILER_ID:MSVC>:/GS->"
    "$<$<CXX_COMPILER_ID:MSVC>:/DWIN32 /D_WINDOWS>"
)

target_compile_features(chaos::runtime INTERFACE
    cxx_std_20
)

# Prebuilt native runtime libraries
# CMAKE_STATIC_LIBRARY_SUFFIX resolves to .lib (Windows) or .a (Linux/macOS).
# chaos_jit is only linked in JIT mode (see conditional block below).
target_link_libraries(chaos::runtime INTERFACE
    "${CHAOS_SDK_ROOT}/lib/chaos_common${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_support${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_fmt${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_runtime_core${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_interpreter${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_bootstrap${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_hot_update${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_eventpipe${CMAKE_STATIC_LIBRARY_SUFFIX}"
    "${CHAOS_SDK_ROOT}/lib/chaos_debugger${CMAKE_STATIC_LIBRARY_SUFFIX}"
)

# chaos_jit is only needed in JIT mode — it references JIT-debug-contract symbols
# (JitDebugContractAddEntry, etc.) that are only available when the full JIT
# debugger module is linked.  In AOT mode, skip it to avoid unresolved externals.
if(CHAOS_IL2CPP_JIT_MODE)
    target_link_libraries(chaos::runtime INTERFACE
        "${CHAOS_SDK_ROOT}/lib/chaos_jit${CMAKE_STATIC_LIBRARY_SUFFIX}"
    )
endif()

# ── chaos::codegen (STATIC IMPORTED) ────────────────────────────────────
# Precompiled generated C++ code (native-aot.generated.cpp -> .lib).
# Consumers link this to get the AOT-compiled method bodies.
#
# Requires chaos::runtime to be linked separately.
add_library(chaos::codegen STATIC IMPORTED)

target_include_directories(chaos::codegen INTERFACE
    "${CHAOS_SDK_ROOT}/include"
)

set_target_properties(chaos::codegen PROPERTIES
    IMPORTED_LOCATION "${CHAOS_SDK_ROOT}/lib/chaos_codegen${CMAKE_STATIC_LIBRARY_SUFFIX}"
)
