# frontend

## 负责

- 承载与前端集成相关的适配代码、工程配置与接口对接实现。
- 维护前端侧与本仓库 contract 的接线层。

## 不负责

- 不负责运行时核心逻辑实现。
- 不负责代码生成器与构建系统的实现细节。

## 与其它目录的边界

- 通过 `contracts/` 中定义的约束与 `runtime/`、`analysis/` 对接。
- 前端构建与打包流程由 `build/` 提供，不在本目录定义。
