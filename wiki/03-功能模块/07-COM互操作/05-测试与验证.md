# COM 互操作测试与验证

## 测试体系

COM 互操作测试位于验证管线 `foundation-dll` 中，覆盖 `System.Private.CoreLib` 和 `System.Runtime.InteropServices` 两个程序集。

### System.Private.CoreLib COM 家族

### ccw-basic 家族

**目录**: `ccw-basic/`
**状态**: ✅ 完成 (V3)

测试 COM Callable Wrapper 的核心功能：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | ISimpleMath.Add 通过 CCW 分发 | 方法调用的正确性 |
| Subject_1 | ISimpleMath.Multiply 通过 CCW 分发 | 方法调用的正确性 |
| Subject_2 | IConstantValue.GetValue 通过 CCW 分发 | 方法调用的正确性 |
| Subject_3 | DualInterfaceImpl (实现两个接口) | 多接口分发的一致性 |

**方法**: 每次调用通过 C# 的接口分发调用两次，比较结果一致性。

### rcw-basic 家族

**目录**: `rcw-basic/`
**状态**: ✅ 完成 (V2)

测试 Runtime Callable Wrapper 的 round-trip 能力和方法分发：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | Round-trip identity | CCW IUnknown* == RCW identity_unknown |
| Subject_1 | Round-trip QI | 通过 RCW QI 返回非空且 != identity_unknown |
| Subject_2 | RCW vtable 方法调用 | 通过 RCW handle 调用 COM 方法（MarshalCallComMethod） |
| Subject_3 | 直通 vtable 调用 | 通过原始 COM 指针调用方法（MarshalCallDirectComMethod） |

**流程**: 托管→创建 CCW→获取 IUnknown*→包装为 RCW→QI 验证→方法分发→释放

### dispatch-basic 家族

**目录**: `dispatch-basic/`
**状态**: ✅ 完成 (V2)

测试 IDispatch 晚期绑定能力：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | IDispatch QI | 通过 QI 获取 IDispatch 接口 |
| Subject_1 | GetIDsOfNames | 按名称查找方法 DISPID |
| Subject_2 | Invoke | 通过 DISPID 调用方法 |

**流程**: 托管→创建 CCW→QI for IDispatch→GetIDsOfNames→Invoke→验证结果

### error-info-basic 家族

**目录**: `error-info-basic/`
**状态**: ✅ 完成 (V1)

测试 COM IErrorInfo 集成和错误信息传播：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | IErrorInfo 格式正确的 HRESULT 异常传播 | COMException 携带 description |
| Subject_1 | IErrorInfo 未设置时回退 hex 格式 | Fallback 行为正确 |
| Subject_2 | GetErrorInfo 跨线程安全 | TLS 隔离 |

**流程**: 模拟 COM 方法返回失败 HRESULT→运行时调用 GetErrorInfo 获取描述→验证异常类型和信息

### System.Runtime.InteropServices COM 家族

**com-types** (`com-types/`) — 22/22 fact tests
- COM 类型系统和接口方法分发验证
- 覆盖 CCW factory 注册、vtable 方法调用、COM 接口类型转换

**com-wrappers** (`com-wrappers/`) — 10/10 fact tests
- ComWrapper 生命周期管理
- 覆盖 ComWrappers API 的跨平台行为

### Objective-C Interop

**objective-c-interop** (`objective-c-interop/`) — 4/4 fact tests

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | CreateReferenceTrackingHandle | GCHandle identity 验证 |
| Subject_1 | Initialize with handler | (needs-manual) 异常传播处理器注册 |
| Subject_2 | SetMessageSendCallback | 消息发送回调注册 |
| Subject_3 | SetMessageSendPendingException | 待处理异常设置 |

## 运行方法

```bash
# 构建管线
dotnet build src/managed/Chaos.IL2CPP.Pipeline

# 构建原生运行时
cmake --build artifacts/presets/debug --target chaos_runtime_core

# 运行全管线验证（生成 codegen、编译、执行、校验）
# --all 标志运行所有 interop 家族（14 个测试族）
run foundation-dll --assembly System.Private.CoreLib --all
run foundation-dll --assembly System.Runtime.InteropServices --all

# 单家族运行
run foundation-dll verify-family --family ccw-basic --assembly System.Private.CoreLib
run foundation-dll verify-family --family rcw-basic --assembly System.Private.CoreLib
run foundation-dll verify-family --family dispatch-basic --assembly System.Private.CoreLib
run foundation-dll verify-family --family error-info-basic --assembly System.Private.CoreLib
run foundation-dll verify-family --family com-types --assembly System.Runtime.InteropServices
run foundation-dll verify-family --family com-wrappers --assembly System.Runtime.InteropServices
```

## 验证清单

| 检查项 | 脚本/命令 |
|--------|----------|
| 托管管线构建 | `dotnet build src/managed/Chaos.IL2CPP.Pipeline` |
| 原生运行时构建 | `cmake --build artifacts/presets/debug --target chaos_runtime_core` |
| CCW QI identity | ccw-basic Subject_0-3 全部 pass |
| RCW round-trip identity | rcw-basic Subject_0 pass |
| RCW QI | rcw-basic Subject_1 pass |
| RCW vtable 方法调用 | rcw-basic Subject_2-3 pass |
| IDispatch QI | dispatch-basic Subject_0 pass |
| IDispatch GetIDsOfNames | dispatch-basic Subject_1 pass |
| IDispatch Invoke | dispatch-basic Subject_2 pass |
| Factory 注册表 | 生成的 hotpatch table 包含 s_ccw_factory_registered_* |

## Native 单元测试

COM 模块的 native 单元测试位于 `testing/src/native/runtime-core/`，基于 GoogleTest 框架，通过 `MarshalTestFixture` 提供最小运行时环境。共 **11 个测试文件，132 个测试用例**（含 9 个 DISABLED），通过 `ctest` 统一运行。

### CCW 测试 (`com_ccw_test.cpp`) — 28 tests

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| QueryInterface 空参数 | 3 | null self/ppv/iid → E_POINTER |
| QueryInterface 接口匹配 | 2 | 无匹配 → E_NOINTERFACE，非零 GUID 无匹配 |
| QueryInterface 注册接口 | 1 | 已注册 GUID → S_OK + ppv 非空 |
| AddRef | 2 | null → 0，已初始化 → value+1 |
| AddRefRelease 周期 | 1 | 完整 AddRef/Release 序列计数验证 |
| Release | 1 | null → 0 |
| CcwFromInterface | 1 | null → null |
| RegisterCcwInterface | 5 | null args → false，有效注册 → interface_count++，满接口表 |
| CreateCcw | 2 | null managed_object → 0 |
| CreateCcwAggregated | 2 | null object/outer → 0 |
| DestroyCcw | 1 | null → no-op |
| Aggregation delegation | 4 | 聚合模式 QI → 委托 outer，AddRef/Release 委托，IUnknown identity → outer |
| IDispatch typeinfo | 2 | GetTypeInfoCount/GetTypeInfo null pctinfo/ppTInfo → E_POINTER |

### RCW 测试 (`com_rcw_test.cpp`) — 17 tests (部分跳过 Win32)

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| IsComRcwHandle | 4 | null → false，valid magic → true，wrong magic → false，random pointer → false |
| FindOrCreateRcw | 3 | null → null，valid → RCW with magic，same ptr → same RCW |
| ReleaseRcw | 3 | null → no-op，refcount double release，refcount=0 → no-op |
| QueryInterfaceCached | 4 | null rcw/iid → null，empty cache → null，cache hit，cache hit second entry |
| IsComRcwHandle after Create | 1 | FindOrCreateRcw 后验证 magic |

### ConnectionPoint 测试 (`com_connection_point_test.cpp`) — 13 active + 9 DISABLED

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| ComFindConnectionPoints | 3 | null handle/iid → E_POINTER |
| ComAdvise | 4 | null handle/iid/sink/cookie → E_POINTER |
| ComUnadvise | 3 | null handle → E_POINTER，null iid → no conn |
| ComCreateEventSinkCcw | 2 | null delegate/iid → 0 |
| CCW FindConnectionPoints | 3 | no match → NOCONNECTION，IUnknown match，registered IID match |
| Unadvise lifecycle | 1 | 无 prior Advise → NOCONNECTION |
| DISABLED (needs MemoryDomain) | 9 | Advise/Unadvise 完整周期、多 sink、readvise、不存在 cookie、跨 IID |

### Platform 测试 (`com_platform_test.cpp`) — 13 tests

COM 平台抽象层验证。

### Factory 测试 (`com_factory_test.cpp`) — 10 tests

CCW Factory 运行时注册表验证。

### TypeLib 测试 (`com_typelib_test.cpp`) — 8 tests

ITypeInfo + ITypeLib vtable 验证。

### Dispatch 测试 (`com_dispatch_test.cpp`) — 8 tests

IDispatch Invoke 单元测试。

### Stress 测试

| 测试文件 | 数量 | 验证点 |
|---------|------|--------|
| com_ccw_stress_test | 4 | CCW 并发创建/释放 |
| com_rcw_stress_test | 4 | RCW 并发查找/释放 |

### Benchmark 测试 (`com_benchmark_test.cpp`) — 5 tests

CCW/RCW 操作性能基准。

### Integration 测试 (`com_integration_test.cpp`) — 13 tests

端到端 COM dispatch 集成测试（需 chaos_codegen.lib）。

### 运行方法

```bash
# 从 testing 目录构建全部 COM 测试
cmake -S src/native -B build
cmake --build build --config RelWithDebInfo

# 运行全部 COM 测试
ctest --test-dir build -C RelWithDebInfo -R "^com_"

# 单测试运行
./build/runtime-core/RelWithDebInfo/com_ccw.exe
./build/runtime-core/RelWithDebInfo/com_rcw.exe
./build/runtime-core/RelWithDebInfo/com_connection_point.exe
```
