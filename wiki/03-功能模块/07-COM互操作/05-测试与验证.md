# COM 互操作测试与验证

## 测试体系

COM 互操作测试位于 `testing/foundation-dll/System.Private.CoreLib/` 目录。

### ccw-basic 家族

**目录**: `ccw-basic/`
**状态**: ✅ 完成 (V3)

测试 COM Callable Wrapper 的核心功能：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | ISimpleMath.Add 通过 CCW 分发 | 方法调用的正确性 |
| Subject_1 | ISimpleMath.Multiply 通过 CCW 分发 | 方法调用的正确性 |
| Subject_2 | IConstantValue.GetValue 通过 CCW 分发 | 方法调用的正确性 |
| Subject_3 | DualInterfaceImpl (实现两个接口) | 多接口分发的一致性 |

**方法**: 每次调用通过 C# 的接口分发调用两次，比较结果一致性。

### rcw-basic 家族

**目录**: `rcw-basic/`
**状态**: ✅ 完成 (V2)

测试 Runtime Callable Wrapper 的 round-trip 能力和方法分发：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | Round-trip identity | CCW IUnknown* == RCW identity_unknown |
| Subject_1 | Round-trip QI | 通过 RCW QI 返回非空且 != identity_unknown |
| Subject_2 | RCW vtable 方法调用 | 通过 RCW handle 调用 COM 方法（MarshalCallComMethod） |
| Subject_3 | 直通 vtable 调用 | 通过原始 COM 指针调用方法（MarshalCallDirectComMethod） |

**流程**: 托管→创建 CCW→获取 IUnknown*→包装为 RCW→QI 验证→方法分发→释放

### dispatch-basic 家族

**目录**: `dispatch-basic/`
**状态**: ✅ 完成 (V2)

测试 IDispatch 晚期绑定能力：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | IDispatch QI | 通过 QI 获取 IDispatch 接口 |
| Subject_1 | GetIDsOfNames | 按名称查找方法 DISPID |
| Subject_2 | Invoke | 通过 DISPID 调用方法 |

**流程**: 托管→创建 CCW→QI for IDispatch→GetIDsOfNames→Invoke→验证结果

### error-info-basic 家族

**目录**: `error-info-basic/`
**状态**: ✅ 完成 (V1)

测试 COM IErrorInfo 集成和错误信息传播：

| Subject | 测试内容 | 验证点 |
|---------|---------|--------|
| Subject_0 | IErrorInfo 格式正确的 HRESULT 异常传播 | COMException 携带 description |
| Subject_1 | IErrorInfo 未设置时回退 hex 格式 | Fallback 行为正确 |
| Subject_2 | GetErrorInfo 跨线程安全 | TLS 隔离 |

**流程**: 模拟 COM 方法返回失败 HRESULT→运行时调用 GetErrorInfo 获取描述→验证异常类型和信息

## COM 套间测试

COM 套间支持（STA/MTA）通过 `run foundation-dll verify-family --family com-apartment-basic` 验证，覆盖 CoInitializeEx、套间类型查询、STA message pump 等场景。

## SAFEARRAY 测试

SAFEARRAY marshaling 通过 struct marshaling 管线验证，覆盖 SafeArrayCreate/SafeArrayAccessData/SafeArrayUnaccessData/SafeArrayDestroy 四个 OLEAUT32 thunk 函数，以及 Variant VT_ARRAY 的 boxing/unboxing 路径。

## 运行方法

```bash
# 构建管线
dotnet build src/managed/Chaos.IL2CPP.Pipeline

# 构建原生运行时
cmake --build artifacts/presets/debug --target chaos_runtime_core

# 运行全管线验证（生成 codegen、编译、执行、校验）
run foundation-dll verify-family --family ccw-basic
run foundation-dll verify-family --family rcw-basic
run foundation-dll verify-family --family dispatch-basic
run foundation-dll verify-family --family error-info-basic
run foundation-dll verify-family --family com-apartment-basic
```

## 验证清单

| 检查项 | 脚本/命令 |
|--------|----------|
| 托管管线构建 | `dotnet build src/managed/Chaos.IL2CPP.Pipeline` |
| 原生运行时构建 | `cmake --build artifacts/presets/debug --target chaos_runtime_core` |
| CCW QI identity | ccw-basic Subject_0-3 全部 pass |
| RCW round-trip identity | rcw-basic Subject_0 pass |
| RCW QI | rcw-basic Subject_1 pass |
| RCW vtable 方法调用 | rcw-basic Subject_2-3 pass |
| IDispatch QI | dispatch-basic Subject_0 pass |
| IDispatch GetIDsOfNames | dispatch-basic Subject_1 pass |
| IDispatch Invoke | dispatch-basic Subject_2 pass |
| Factory 注册表 | 生成的 hotpatch table 包含 s_ccw_factory_registered_* |

## Native 单元测试

COM 模块的 native 单元测试位于 `testing/src/native/runtime-core/`，基于 GoogleTest 框架，通过 `MarshalTestFixture` 提供最小运行时环境。

### CCW 测试 (`com_ccw_test.cpp`) — 20 tests

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| QueryInterface 空参数 | 3 | null self/ppv/iid → E_POINTER |
| QueryInterface 接口匹配 | 1 | 无匹配 → E_NOINTERFACE |
| AddRef | 2 | null → 0，已初始化 → value+1 |
| Release | 1 | null → 0 |
| CcwFromInterface | 2 | null → null，entry path → CCW pointer |
| RegisterCcwInterface | 5 | null args → false，已满 → false，有效注册 → interface_count++ |
| CreateCcw | 2 | null managed_object → 0 |
| CreateCcwAggregated | 2 | null object/outer → 0 |
| DestroyCcw | 1 | null → no-op |
| IDispatch | 2 | GetTypeInfoCount null pctinfo → E_POINTER，GetTypeInfo null ppTInfo → E_POINTER |

### RCW 测试 (`com_rcw_test.cpp`) — 7 pass + 5 skip on Win32

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| IsComRcwHandle | 3 | null → false，valid magic → true，wrong magic → false |
| FindOrCreateRcw | 2 | null → null (Win32 skip)，valid → RCW with magic |
| ReleaseRcw | 2 | null → no-op，refcount double release (Win32 skip) |
| QueryInterfaceCached | 3 | null rcw/iid → null，empty cache → null (Win32 skip) |
| IsComRcwHandle after Create | 1 | (Win32 skip) |
| FindOrCreateRcw dedup | 1 | (Win32 skip) |

### ConnectionPoint 测试 (`com_connection_point_test.cpp`) — 10 tests

| 测试类别 | 数量 | 验证点 |
|---------|------|--------|
| ComFindConnectionPoints | 3 | null handle/iid → E_POINTER |
| ComAdvise | 4 | null handle/iid/sink/cookie → E_POINTER |
| ComUnadvise | 3 | null handle → E_POINTER，null cookie → not found

### 运行方法

```bash
# Build
cmake --build artifacts/presets/debug --target com_ccw
cmake --build artifacts/presets/debug --target com_rcw
cmake --build artifacts/presets/debug --target com_connection_point

# Run
./build/testing/src/native/runtime-core/Debug/com_ccw.exe
./build/testing/src/native/runtime-core/Debug/com_rcw.exe
./build/testing/src/native/runtime-core/Debug/com_connection_point.exe
```
