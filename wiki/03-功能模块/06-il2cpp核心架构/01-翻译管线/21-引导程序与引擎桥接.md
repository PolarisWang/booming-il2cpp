# 引导程序与引擎桥接

## 引导程序（Bootstrap）

### 核心职责

管理运行时的代码注册和初始状态。在 AOT 主模块加载时执行一次性的引导流程。

### BootstrapState

| 字段 | 说明 |
|------|------|
| `code_registration` | 代码生成注册表指针 |
| `metadata_registration` | 元数据注册表指针 |
| `options` | 运行时选项 |
| `aot_image_handle` | AOT 根模块的 ImageHandle |
| `aot_domain_id` | AOT 主模块的内存域 ID |
| `is_registered` | RegisterCodegen 是否已完成 |
| `is_bootstrapped` | BootstrapRuntime 是否已完成 |

### 访问器

| 函数 | 说明 | 定义位置 |
|------|------|---------|
| `PeekBootstrapState()` | 读取当前引导状态 | bootstrap.cpp |
| `GetCodegenBridgeV0()` | 获取 codegen bridge 实例 | bootstrap.cpp |
| `FindRegisteredTypeCapabilityEntry()` | 查找已注册的类型能力条目 | bootstrap.cpp |
| `FindInvokerPointer()` | 查找调用器函数指针 | bootstrap.cpp |
| `FindMethodPointerByToken()` | 按 token 查找方法指针（含 hotupdate 重定向） | bootstrap.cpp |
| `GetAotDomainId()` | 获取 AOT 内存域 ID | bootstrap.cpp |

### 与 MemoryDomain 的关系

- AOT 主模块的内存域在引导期间设置
- AOT domain id 通过 `GetAotDomainId()` 全局可用

### Delegate 系统

Delegate 系统使用 GC 分配（CRAG old-gen）的不可变链表节点实现多播委托：

- **CreateDelegate**: 通过 `FindMethodPointerByToken` 将 metadata token 解析为函数指针，分配单个 DelegateInstance 节点
- **CombineDelegate**: 克隆左链 + 共享右链（O(|left|)）。右链通过引用共享（节点不可变）
- **RemoveDelegate**: 克隆前缀跳过匹配节点（匹配最后一个匹配项），共享后缀
- **DelegateInvoke**: 遍历多播链，每个节点按 `target_instance` 是否存在选择 closed 或 static 调用约定，最后一个返回值胜出（C# 语义）

DelegateInstance 节点结构：

```
method_token | method_pointer | target_instance | next → ... → null
                  ↓                    ↓
              函数指针           managed 对象指针（GC 扫描）
```

### Icall 解析

使用 `std::strstr` 前缀匹配方法名约 ~30 个已知 icall 签名。匹配顺序影响结果——`CoCreateInstanceAggregated` 必须在 `CoCreateInstance` 之前，`ThrowExceptionForHR` 必须在 `GetExceptionForHR` 之前。

字符串常量和签名中**逗号后没有空格**（例如 `"System.Delegate,System.Delegate"`），这是匹配正确性的关键约束。

### 位置

- 模块: `chaos_bootstrap` (STATIC)
- 文件:
  - `src/native/bootstrap/bootstrap.h` — 31 行 public API 头文件，6 个函数声明
  - `src/native/bootstrap/bootstrap_internal.h` — 内部头文件，跨 TU 符号声明
  - `src/native/bootstrap/bootstrap.cpp` — 核心初始化编排 + ABI 入口点 (~640 行)
  - `src/native/bootstrap/bootstrap_delegates.cpp` — Delegate 系统 (~276 行)
  - `src/native/bootstrap/bootstrap_icall.cpp` — Icall 解析 (~203 行)

### 文件拆分说明

Phase D 工业化从原来的单文件 bootstrap.cpp (1084 行) 拆分为 4 个文件：

- **bootstrap.cpp**: 两阶段初始化 (RegisterCodegen → BootstrapRuntime)、CodegenBridgeV0、virtual method 解析、公共 API 实现
- **bootstrap_delegates.cpp**: 全部 delegate 系统（CreateDelegate、CombineDelegate、RemoveDelegate、DelegateInvoke）
- **bootstrap_icall.cpp**: ~30 个 icall 字符串常量和 ResolveIcall 实现
- **bootstrap_internal.h**: 共享类型声明（DelegateInstance、DecodeOpaqueToken）和跨 TU 函数声明

### 测试覆盖

7 个测试目标，25+ 个测试用例：

| 测试目标 | 覆盖内容 |
|----------|----------|
| test_bootstrap_smoke (已有) | pre-init 状态验证 (7 tests) |
| test_bootstrap_stress (已有) | 8 线程 × 100 iter 并发 (1 stress) |
| test_bootstrap_register (新增) | RegisterCodegen 参数校验、happy path、重复注册 (7 tests) |
| test_bootstrap_bootstrap (新增) | BootstrapRuntime 前置条件、完整两阶段 (3 tests) |
| test_bootstrap_delegate (新增) | CreateDelegate/Combine/Remove/Invoke (7 tests) |
| test_bootstrap_resolve (新增) | Virtual 3-tier、TypeCapability、MethodPointer (3 tests) |
| test_bootstrap_icall (新增) | 已知/未知/null icall 解析 (3 tests) |

---

## Codegen Bridge 注册流程

### RegisterCodegen + BootstrapRuntime

AOT 模块启动时依次调用两个 bridge 函数完成引导：

```
entry.exe (或 engine host) 启动
  │
  ├─ 1. RegisterCodegen(CodeRegistrationV0, MetadataRegistrationV0, Options)
  │       ├─ 验证 struct_size 有效性
  │       ├─ 存储 code_registration / metadata_registration / options 指针
  │       ├─ 构建 invoker 查找表 (method_token → invoker_pointer)
  │       └─ 设置 is_registered = true, is_bootstrapped = false
  │
  ├─ 2. BootstrapRuntime()
  │       ├─ 1. 验证 is_registered 和 GetRuntimeAbiV0()
  │       ├─ 2. 注册 JIT SEH vectored exception handler
  │       ├─ 3. 注册 AOT 泛型类型/方法实例化
  │       ├─ 4. 注册 AOT method entries (QueryAotMethod)
  │       ├─ 5. 注册 AOT root memory domain
  │       ├─ 6. 解析 external runtime dispatch table (ChaosResolveExternalRuntimeFnTable)
  │       ├─ 7. 注册 AOT VTable descriptors
  │       ├─ 8. 注册 GC slot maps
  │       ├─ 9. 确保 aot_image_handle 已设置
  │       └─ 10. 设置 is_bootstrapped = true
  │
  └─ 引导完成 → 可执行 managed 代码
```

### CodegenBridgeV0 (15 函数指针 ABI vtable)

通过 `chaos_codegen_get_bridge_v0()` C 导出，注册在 process-wide kCodegenBridgeV0：

| 索引 | 函数 | 说明 | 定义位置 |
|------|------|------|---------|
| 0 | — | `CHAOS_CODEGEN_BRIDGE_V0` (magic) | 常量 |
| 1 | — | `sizeof(CodegenBridgeV0)` | 常量 |
| 2 | `RegisterCodegen` | 第一阶段初始化 | bootstrap.cpp |
| 3 | `BootstrapRuntime` | 第二阶段初始化（10 步） | bootstrap.cpp |
| 4 | `ResolveTypeByToken` | type_token → TypeInfoHandle | bootstrap.cpp |
| 5 | `QueryTypeCapability` | 查询类型能力 | bootstrap.cpp |
| 6 | `ResolveMethodByToken` | method_token → MethodInfoHandle | bootstrap.cpp |
| 7 | `ResolveFieldByToken` | field_token → FieldInfoHandle | bootstrap.cpp |
| 8 | `BoxValue` | 值类型装箱 | bootstrap.cpp |
| 9 | `UnboxValue` | 值类型拆箱 | bootstrap.cpp |
| 10 | `ResolveVirtualMethod` | 3-tier 虚方法解析 | bootstrap.cpp |
| 11 | `InvokeVirtual` | 虚方法调用 | bootstrap.cpp |
| 12 | `ResolveVirtualMethodByToken` | token 版虚方法解析 | bootstrap.cpp |
| 13 | `CreateDelegate` | 创建委托 | bootstrap_delegates.cpp |
| 14 | `DelegateInvoke` | 委托调用 | bootstrap_delegates.cpp |
| 15 | `ResolveIcall` | icall 解析 | bootstrap_icall.cpp |

### Codegen 生成的数据

codegen 在 `native-aot.generated.cpp` 中生成以下数据结构：

| 数据 | 类型 | 用途 |
|------|------|------|
| `kAotMethods[]` | `void*[]` | 所有 AOT 编译方法函数指针表 |
| `g_dispatch_table[]` | `HotpatchEntryV0[]` | 热更新 dispatch 表 |
| `g_type_index[]` | `HotpatchTypeEntryV0[]` | 类型名→方法索引 |
| `g_method_index[]` | `HotpatchMethodEntryV0[]` | 方法名→token 映射 |
| `g_token_slot[]` | `HotpatchSlotEntryV0[]` | token→dispatch slot |
| `kAotDirectFnTable[]` | `void*[]` | external runtime 函数指针表 |
| `kAotSubjectIds[]` | `const char*[]` | subjectId 字符串表 |
| `CodeRegistrationV0` | struct | codegen 注册束 |
| `MetadataRegistrationV0` | struct | 元数据注册束（泛型/字段/metadata） |
| `CodegenRegistrationOptionsV0` | struct | 注册选项（image_name 等） |
| `ReflectionQueryImageDescriptor` | struct | 反射查询描述符（module.image） |

### SetAotBridge

在 `ApplyPatchFromMemory` 中调用，为 HotpatchNameRegistry 设置 bridge + aot_image：

```
ApplyPatchFromMemory(.patchdata)
  │
  ├─ 1. GetCodegenBridgeV0() → bridge
  ├─ 2. PeekBootstrapState() → code_registration
  ├─ 3. code_registration → image → ReflectionQueryImageDescriptor
  ├─ 4. HotpatchNameRegistry::SetAotBridge(bridge, aot_image_handle)
  │       └─ interpreter 的 ResolveSubjectId 通过 bridge→resolve_*_by_token 查询
  │
  └─ 5. 继续加载补丁方法
```

`SetAotBridge` 替代了旧版 `InterpreterEntryDirect::Step0_Bridge` 每次调用都设置 bridge 的方式。现在 bridge 在 patch 加载时只设置一次。

### AotDirectFn 表注册

`ChaosRegisterDirectFnTable(subjects, table, count)` 由 codegen 生成的 C++ 代码自动调用：

```cpp
// codegen 生成
extern "C" const void* kAotDirectFnTable[] = { ... };
extern "C" const char* kAotSubjectIds[] = { ... };
extern "C" const int kAotDirectFnCount = ...;

// 静态初始化时自动注册
struct AotDirectFnRegistrar {
    AotDirectFnRegistrar() {
        ChaosRegisterDirectFnTable(kAotSubjectIds, kAotDirectFnTable, kAotDirectFnCount);
    }
} g_aot_direct_fn_registrar;
```

注册后，`aot_core_ir_reader.cpp` 在 `DeserializeAotCoreIr` 的 `ResolveDirectFn` 回调中通过 subjectId 二分查找找到对应的函数指针，填入 `IRInstruction::direct_fn`。

### 三种运行场景对比

| 场景 | RegisterCodegen 调用者 | SetAotBridge 时机 | 特点 |
|------|----------------------|-------------------|------|
| **真实 host (游戏引擎)** | host 调用 `RegisterCodegen` + `BootstrapRuntime` | 内置在 BootstrapRuntime 中 | 完整 reflection query model |
| **entry.exe 测试管线** | `runtime-entry.cpp` 直接调用 `RegisterCodegen` + `BootstrapRuntime` | `ApplyPatchFromMemory` 内调用 | bypass 脚本不再剥离 code registration |
| **native unit test** | 测试 fixture 手动构造注册 | 按需调用 | 可控环境 |

---

## 引擎桥接（Engine Bridge）

### 核心职责

连接运行时核心和引导程序到引擎主机。实现 engine contracts（lifecycle.h、ownership.h、callback.h）的 C++ 侧。

### 桥接函数

| 函数 | 说明 |
|------|------|
| `CreateEngineObjectHandle(object)` | 创建引擎对象句柄 |
| `ResolveEngineObjectHandle(handle)` | 解析引擎对象句柄为指针 |
| `RegisterEngineLifecycleCallback(callback)` | 注册引擎生命周期回调 |
| `DispatchEngineLifecycleCallbacks(event)` | 分发生命周期事件 |
| `IsMainThreadLane()` | 检查是否为主线程通道 |

### Engine Contracts 实现

| 合约 | 桥接实现 |
|------|---------|
| `lifecycle.h` | engine_init/shutdown/reload → `RegisterEngineLifecycleCallback` + `DispatchEngineLifecycleCallbacks` |
| `ownership.h` | engine_retain/release/transfer → `CreateEngineObjectHandle` / `ResolveEngineObjectHandle` |
| `callback.h` | engine_register_callback / engine_dispatch_callback → 桥接分发 |

### 位置

- 模块: `chaos_engine_bridge`
- 文件: `src/native/engine-bridge/engine_bridge.*`
