# 活动任务优先

## 规则

只要 `docs/dev/ACTIVE.md` 存在，任何问答前都必须先处理当前 active 任务。

## 用户选项

- `继续`
- `挂起`
- `放弃`

## 结果

- `继续`：回到当前任务执行
- `挂起`：保存进度并将任务目录移到 `docs/dev/hanging/` 后再进入新问题
- `放弃`：将任务目录移到 `docs/dev/abandoned/` 后再进入新问题

## 最近更新

- `2026-04-04`：切换到 active 任务优先规则。
