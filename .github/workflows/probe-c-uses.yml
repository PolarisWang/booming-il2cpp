name: Probe C Vars Uses
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag'
        required: true
        type: string
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@${{ vars.ACTIONS_CHECKOUT_VER || 'v4' }}
      - run: echo ok
