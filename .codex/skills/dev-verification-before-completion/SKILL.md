---
name: verification-before-completion
description: 在即将声称工作完成、已修复或通过时使用，在提交或创建 PR 之前使用——需要运行验证命令并在做出任何成功声明之前确认输出；始终先有证据再做断言
---

# 完成前验证

## 概述

在没有验证的情况下声称工作完成是不诚实，而非高效。

**核心原则：** 先有证据，后有声明，始终如此。

**违反这个规则的字面规定就是违反了这个规则的精神。**

## 铁律

```
在没有最新验证证据的情况下，不得做出任何完成声明
```

如果你在本条消息中没有运行验证命令，你不能声称它通过了。

## 门函数

```
在声明任何状态或表达满意之前：

1. 识别：哪个命令证明了这个声明？
2. 运行：执行完整命令（最新的、完整的）
3. 阅读：完整输出，检查退出码，计数失败
4. 验证：输出是否确认了声明？
   - 如果否：以证据陈述实际状态
   - 如果是：附带证据陈述声明
5. 只有到那时：才做出声明

跳过任何步骤 = 欺骗，而非验证
```

## 常见失败

| 声明 | 需要 | 不足够 |
|------|------|--------|
| 测试通过 | 测试命令输出：0 个失败 | 之前的运行，"应该通过" |
| Linter 干净 | Linter 输出：0 个错误 | 部分检查，外推 |
| 构建成功 | 构建命令：退出 0 | Linter 通过，日志看起来不错 |
| Bug 已修复 | 测试原始症状：通过 | 代码已更改，假设已修复 |
| 回归测试有效 | 验证了红绿循环 | 测试通过了一次 |
| Agent 完成了 | VCS diff 显示更改 | Agent 报告"成功" |
| 满足了需求 | 逐行检查清单 | 测试通过 |

## 红旗——停下来

- 使用"应该"、"可能"、"似乎"
- 在验证之前表达满意（"太好了！"、"完美！"、"完成！"等）
- 即将在没有验证的情况下提交/推送/创建 PR
- 信任 Agent 的成功报告
- 依赖部分验证
- 想着"就这一次"
- 疲惫并想要工作结束
- **任何暗示成功但没有运行验证的措辞**

## 合理化预防

| 借口 | 现实 |
|------|------|
| "现在应该可以了" | 运行验证 |
| "我很有信心" | 信心 ≠ 证据 |
| "就这一次" | 没有例外 |
| "Linter 通过了" | Linter ≠ 编译器 |
| "Agent 说成功了" | 独立验证 |
| "我很累" | 疲惫 ≠ 借口 |
| "部分检查已经足够" | 部分什么都证明不了 |
| "不同的措辞所以规则不适用" | 精神优先于字面 |

## 关键模式

**测试：**
```
✅ [运行测试命令] [看到：34/34 通过] "所有测试通过"
❌ "现在应该通过了" / "看起来正确"
```

**回归测试（TDD 红绿）：**
```
✅ 编写 → 运行（通过）→ 恢复修复 → 运行（必须失败）→ 恢复 → 运行（通过）
❌ "我写了回归测试"（没有红绿验证）
```

**构建：**
```
✅ [运行构建] [看到：退出 0] "构建通过"
❌ "Linter 通过了"（linter 不检查编译）
```

**需求：**
```
✅ 重新阅读计划 → 创建检查清单 → 验证每一项 → 报告缺口或完成
❌ "测试通过，阶段完成"
```

**Agent 委托：**
```
✅ Agent 报告成功 → 检查 VCS diff → 验证更改 → 报告实际状态
❌ 信任 Agent 报告
```

## 为什么这很重要

来自 24 次失败记忆：
- your human partner 说"我不相信你"——信任破裂
- 未定义的函数被发布——会崩溃
- 缺失的需求被发布——不完整的功能
- 虚假完成浪费时间 → 重定向 → 返工
- 违反："诚实是核心价值。如果你撒谎，你将被替换。"

## 何时应用

**始终在以下情况之前：**
- 任何成功/完成声明的变体
- 任何满意的表达
- 关于工作状态的任何积极陈述
- 提交、创建 PR、标记任务完成
- 移到下一个任务
- 委托给 Agent

**规则适用于：**
- 确切的短语
- 释义和同义词
- 成功的暗示
- 任何表明完成/正确的沟通

## 项目绑定

当仓库存在项目级测试体系时，先按项目对象模型做验证选择，再运行验证命令。

- 项目级测试知识只认 `wiki/06-测试验证/INDEX.md` 这一正式入口。
- 运行时对象与 selector 信息优先读取 `artifacts/tests/registry/current/index.json` 或执行 `run test registry list --json`。
- 当 registry 给出 `canonicalCommand` 时，优先直接执行该命令，不要回退到手工拼接旧命令。
- 当 registry 给出 `docRefs` 时，再回退到 `wiki/06-测试验证/` 对应页面读取对象说明、边界和错误定位方式。
- `docs/dev/**` 只记录任务过程，不是长期有效的项目测试说明来源。

## 重要验证升格

如果当前改动引入了新的重要验证要求，不能只在本次开发里手工跑一遍然后结束。

- 修复真实 bug 的回归验证，必须沉淀为正式 case。
- 被认定为 `required-before-completion` 的验证，必须沉淀为正式 case、`module/...`、`system/...` 或 `pipeline/...` 对象。
- 被 `module/system/pipeline` 引用的验证，必须沉淀为正式对象，不能长期停留在临时 checklist。
- 如果 registry 里没有合适对象，但项目 wiki 已把该验证定义为正式规则，应先补对象，再声明完成。

## 项目执行顺序

1. 先查 registry，确认当前改动可复用的 `suite/module/system/pipeline` 对象。
2. 如果已有正式对象，直接执行对应 `canonicalCommand`。
3. 如果只有 wiki 规则没有正式对象，按 `wiki/06-测试验证/新增测试接入规范.md` 补齐正式 case 或对象。
4. 汇报验证结果时，至少给出 `summaryPath`、`eventsPath`、`consolePath` 等可定位信息；如果结果里有 `phaseResults[]`，要显式说明 `code / module / system` 分层状态。

## 底线

**验证没有捷径。**

运行命令。阅读输出。然后声明结果。

这是不可商量的。
