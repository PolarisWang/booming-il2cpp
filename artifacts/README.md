# artifacts

## 负责

- 收口 build 目录输出。
- 收口日志、打包产物、运行期快照副本与 diff 输出。

## 不负责

- 不保存权威 snapshot。
- 不保存第三方源码或长期契约文档。

## 与其它目录的边界

- 权威 snapshot 应保留在源码树中的权威位置，不放在 `artifacts/`。
- 本目录内容默认视为可再生输出，由 `.gitignore` 规则防止误提交。

