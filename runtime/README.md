# runtime

## 负责

- 承载运行时核心能力、桥接层与平台执行相关实现。
- 落实 native/ABI 行为契约并对接平台适配。

## 不负责

- 不负责上游源码语义分析或代码生成策略决策。
- 不负责第三方依赖清单管理。

## 与其它目录的边界

- 接收 `codegen/` 产物与 `contracts/` 约束，向 `tests/` 暴露可验证行为。
- 平台构建与打包门禁由 `build/` 与架构文档定义。
