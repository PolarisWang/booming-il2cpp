# third_party

## 负责

- 仅承载 vendored 第三方源码快照（source snapshot）。
- 维护每个依赖目录自带的来源、版本、许可证与用途说明。

## 不负责

- 不存放工具链、下载缓存、构建产物、临时文件。
- 不承载运行日志、打包结果或测试输出。

## 与其它目录的边界

- 默认接入方式是手动 vendor snapshot，不在本目录执行自动下载流程。
- 每个依赖子目录必须包含 `README.md`，并在根级 `INDEX.md` 登记摘要。
- 平台构建输出统一进入 `artifacts/`，不得写入本目录。

